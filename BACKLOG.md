# BACKLOG

Out of scope for this session, in writing (D-051). Every row carries `Raised in WP`,
`Blocks = No`, and an `Acceptance` command copied verbatim from the section that owns it.
A `DONE` row names the WP that closed it and keeps its acceptance command, now live.

| ID | Item | Owner | Raised in WP | Blocks | Acceptance |
|---|---|---|---|---|---|
| BL-001 | Three-arm SAA comparison and the token/step cost claim | §6 P1.3 | WP-0 | No | `node packages/sfs/tools/cost-delta.mjs --arms 3` exits 0 with 8-10 cases per arm |
| BL-002 | Round-trip over all 12 corpus forms. 7 are clean at rate 1.00; the last four await BL-024 | §2.5 | WP-0 | No | `npm run sfs -- roundtrip --scope packages/sfs/config/roundtrip-expected.json` -> exit 0, `12 of 12` |
| BL-003 | DONE (WP-3b.3/3b.4, D-109): T3 and its placement predicates ship at `packages/verify/src/tiers/t3-semantic.mjs`, `g-check-references` under `src/gates/`; `quarantine/` is deleted | §3.3 | WP-0 | No | `node packages/verify/src/verify.mjs .build/wp3b --screen inline-editable-table --tiers t1,t2,t3` -> exit 0 |
| BL-004 | DONE (WP-2b, D-113): the registry is source-parsed from framework TypeScript at a pinned ref, measuring `full 93/121` | §2.8 | WP-0 | No | `node packages/registry/src/validate.mjs` -> `priority full 13/13 · full >= 93/121` |
| BL-005 | DONE (WP-3c/WP-3d, D-127/D-128): T4 smoke and T4b residue ship at `packages/verify/src/tiers/`, T5 at `t5-visual.mjs`; D-015 closes to `g-t5-advisory`. The LIVE paths are BL-033/BL-034 | §3.2.5, §3.6 | WP-0 | No | `node packages/verify/src/tiers/t4-smoke.mjs --selftest --json` -> exit 0 |
| BL-006 | Real 23-field envelope validation against a backend export | §2.1.2 | WP-0 | No | `node packages/verify/src/tiers/t1-schema.mjs --envelope <real-export>.json` -> exit 0, `file family checked` |
| BL-007 | DONE (WP-7): the three design skills are thin; `prose-budget.json` carries zero waivers | §4.4 | WP-0 | No | `node packages/verify/src/gates/g-prose-budget.mjs` -> exit 0 with 0 waivers whose `until` is BL-007 |
| BL-008 | `.claude/hooks/**`, `.mcp.json`, the agent roles, `enabledPlugins`. Implemented in WP-8a..WP-8d; the row survives only because D-017 and D-022 carry `pending:BL-008` and deleting it hard-fails `g-decisions`. It closes when those name live enforcers | §4.3 | WP-0 | No | After a session RESTART: plant a `.form.json` Write and observe the denial; `node packages/verify/src/gates/g-githook-contract.mjs` -> exit 0 |
| BL-009 | DONE (WP-9, D-110): shape-indexed retrieval ships at `packages/precedent/src/{index,shape}.mjs`; Index A retrieves over 11 corpus forms | §4.7 | WP-0 | No | `node --test packages/precedent/test/` -> exit 0 with retrieval tests, no `E_NOT_IMPLEMENTED` |
| BL-010 | DONE: `tsconfig.json` carries `noUncheckedIndexedAccess: true` | D-024 | WP-0 | No | `npm run typecheck` -> exit 0 with `noUncheckedIndexedAccess: true` in tsconfig.json |
| BL-011 | Split the brief bundle to <= 61440 B. **Permanently deferred by D-114**, not pending: unreachable by the sanctioned extraction, since roughly half the bundle is design prose that cannot move to `data/*.json` | §5.2 D-046 | WP-0 | No | `node packages/verify/src/gates/g-brief-budget.mjs` -> exit 0 with `bundle.enforced: true` and total <= 61440 |
| BL-012 | DONE (WP-7): the prose debt in the non-design skills is cleared and `add-public-portal`'s frontmatter matches its folder | §1.1 | WP-0 | No | `node packages/verify/src/gates/g-prose-budget.mjs` -> exit 0 with 0 waivers whose `until` is BL-012 |
| BL-014 | Property-based compiler fuzzer: generate valid SFS documents from `sfs.schema.json` and assert the §2.6 predicates plus Q5 determinism on each, testing the invariants over a generated population | §2.4 | WP-1.a | No | `node --test packages/sfs/test/fuzz.test.mjs` -> exit 0 with `cases >= 200` and 0 predicate failures |
| BL-015 | HALF DONE. The differential oracle ships: `packages/sfs/test/oracle.test.mjs` runs P3 over the declared subset. The CI half does not — there is no `.github/workflows/` directory, and that file is the repository owner's to author | §2.4.5 | WP-1.a | No | `node --test packages/sfs/test/oracle.test.mjs` -> exit 0 over 6 forms, and the workflow file runs `npm run green` with no `continue-on-error` |
| BL-020 | DONE (WP-2b, D-113): §2.8's alias for BL-004; one all-121 source-parse closed both | §2.8.4 | WP-2 | No | `node packages/registry/src/validate.mjs` -> `full >= 93/121` |
| BL-021 | Lift legacy `columns`/`sizableColumns` into `row` + `responsive.fixed` on decompile. Both are `authorable:false` under D-112, so a legacy form using them produces a structural escape. `standalone-create` is the one declared escape in `roundtrip-expected.json`, which also names a Submit action config the grammar cannot express | D-112 | WP-2 | No | `npm run sfs -- roundtrip` with `standalone-create` moved to `expect: clean` -> rate stays 1.00 |
| BL-022 | Establish framework versions for the 7 authorable-but-version-unknown types (`buttons`, `dynamicView`, `imagePicker`, `logViewer`, `permissionTagGroup`, `processMonitor`, `threeStateSwitch`) and flip each from `authorable:false` (D-114) to authorable | §2.8.4 | WP-2 | No | `node packages/registry/src/validate.mjs` -> `deferredAuthorable < 7` and every flipped type has a non-null version |
| BL-023 | Compiler emits `resolvedFrom` colour provenance, so T2.17 verifies at the output tier that each hex is a resolved brand token rather than disposing na | §2.3 | WP-3a.2 | No | `node packages/verify/src/tiers/t2-registry.mjs` with a hardcoded-hex fixture -> T2.17 fails |
| BL-024 | Lift the three node-types the four triage-only corpus forms escape on: `sectionSeparator`, `collapsiblePanel`->`panel`, `tabs`. Each needs an `sfsNode` overlay + compiler expansion + decompiler lift; `tabs` also feeds T3's tabKey. Non-corpus constructs stay backlog: `statusTag`, `title`, `button`, `alert`, `wizard`, `htmlRender`, UNEQUAL-span `columns` (D-101 lifts equal spans) | §2.5 | WP-5e | No | `npm run sfs -- roundtrip` over a form using each construct -> that construct lifts and contributes zero structural escapes |
| BL-033 | The live T4/T4b path: drive a real Shesha frontend against a running backend, click every action site and read each consequence back. `--selftest` proves the six assertions against a stub, never the transport, which is untested until this runs. BLOCKED B14 | §3.10 | WP-3c | No | `npx playwright install chromium`, then `node packages/verify/src/verify.mjs <run> --tiers t4 --base-url <url>` against a host where clicking is not a live mutation -> `actions`/`consequences` non-zero |
| BL-034 | Qualify a judge model on 10 anchors before T5's advisory verdict is trusted: agreement against human-scored anchors, measured and recorded. Blocked on inputs, not code — 10 accepted screens with design sources do not exist as a committed set | §3.10 | WP-3c | No | `node packages/verify/src/gates/g-judge-isolation.mjs` -> exit 0 with an anchor set of 10 and recorded agreement |
| GAP-001 | DONE (WP-6, D-111): the action grammar gained `openDialog` argIgnore + `closeDialog`/`confirm`/`startEdit`/`cancelEdit`, so production dialog/edit/column configs lift; employee-table, employee-create and rs-table moved to declaredSubset clean at rate 1.00 | §2.1.7 | WP-5.b | No | `npm run sfs -- roundtrip` with employee-table in declaredSubset clean -> rate stays >= 0.90 |
| GAP-nnn | Compiler gaps discovered in WP-5 triage | §2.5 | WP-5 | No | Each carries a `test.todo` fixture under `packages/sfs/test/fixtures/gaps/` naming its id |
| PROM-nnn | SFS promotions discovered in WP-5 triage | §2.5 | WP-5 | No | Each carries prop names and the count of forms needing it |

## Notes

BL-011 is the one deferral that changes a gate's declared scope rather than adding work:
`g-brief-budget` enforces `CONTROL.md <= 25600 B` (D-060, override O2) and measures the
bundle against its 61440 B target without failing on it. D-114 settles it by removing
WP-16b from Scope B, so `bundle.enforced` stays `false` permanently, not pending a WP.
BLOCKED.md B11 carries the summing command; its byte figure is dated evidence.
