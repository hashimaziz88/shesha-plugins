{
  "_meta": {
    "version": 1,
    "purpose": "Single-source rule registry (Shesha 0.45-only). Every mechanical fact lives HERE once; validators load this file and cite rule ids; prose docs cite [R-xxx] instead of restating. severity: fail = blocks push (machine-checked where check != null), warn = flagged, process = judgment/process rule enforced by the skill flow.",
    "generation": "0.45",
    "sources": "non-negotiables.md, renderer-physics.md, modern-renderer-gotchas.md (merged into renderer-physics.md), validate-guardrails.js, design-pipeline firm rules — consolidated 2026-07-22"
  },
  "rules": [
    { "id": "R-001", "group": "structure", "severity": "fail", "check": "walker: every non-root component has parentId === direct parent id; root components carry parentId 'root'",
      "statement": "Every component carries `parentId` set to its direct parent's `id` (root-level components get \"root\"). Missing parentId crashes the renderer with no useful error." },
    { "id": "R-002", "group": "structure", "severity": "fail", "check": "walker: component id is a UUID or nanoid (≥10 chars, mixed alphabet) — short placeholder ids fail",
      "statement": "Every component `id` is a generated unique id — a UUID or a nanoid (the 0.45 designer emits nanoids). Short human-typed placeholder ids (btn1, pr2) are silently ignored — the form renders blank." },
    { "id": "R-003", "group": "versioning", "severity": "fail", "check": "walker: component.version is an integer equal to the KB version for its type",
      "statement": "Every component carries its type's CURRENT integer `version` from the 0.45 components-kb. No version → legacy render path (inputs render as read-only spans) or the migration chain re-runs and throws; a stale version SILENTLY DROPS the whole desktop style block." },
    { "id": "R-004", "group": "binding", "severity": "fail", "check": "walker: propertyName (incl. datatable column propertyName) starts lowercase",
      "statement": "Every `propertyName` is camelCase — including datatable column propertyNames. Metadata returns PascalCase paths but GQL field keys are camelCase; a PascalCase column fetches rows yet renders blank cells." },
    { "id": "R-005", "group": "data", "severity": "fail", "check": "walker: every datatable/datalist has a dataContext ancestor carrying entityType (string) + sourceType 'Entity'",
      "statement": "The data wrapper for datatable/datalist is `dataContext` (v8) carrying explicit `entityType` (fullClassName STRING), `sourceType: \"Entity\"`, `dataFetchingMode: \"paging\"`, `defaultPageSize`, `uniqueStateId`, `componentName`, `propertyName`. It does NOT inherit from formSettings.modelType; a bare dataContext 500s on page load." },
    { "id": "R-006", "group": "structure", "severity": "fail", "check": "walker: if any component has validate.required === true, a validationErrors component exists in the tree",
      "statement": "A `validationErrors` component is ALWAYS in the tree (conventionally just above the action row) whenever the form has any required input — otherwise a failed submit renders nothing." },
    { "id": "R-007", "group": "actions", "severity": "fail", "check": "walker: form actions only inside buttonGroup items; exactly one primary per group; Submit item carries actionName 'Submit' + actionOwner 'shesha.form'; Submit is paired with an exit (Navigate/Cancel) item",
      "statement": "Form action buttons live in ONE `buttonGroup`, never standalone `button` components. Save/Submit MUST carry `actionConfiguration: { actionName: \"Submit\", actionOwner: \"shesha.form\" }` and be paired with an exit button (Back/Close/Cancel). Downstream tooling infers form intent from buttonGroup items — a mis-wired Submit misreads the whole form." },
    { "id": "R-008", "group": "actions", "severity": "fail", "check": "walker: every Navigate action carries non-empty target/targetUrl",
      "statement": "Every Navigate action carries a non-empty `target`/`targetUrl`. An empty target renders <Link href=undefined> and crashes the page. Mustache in targets must be resolvable in its runtime context ({{selectedRow.id}} on row actions, {{data.id}} on form actions)." },
    { "id": "R-009", "group": "structure", "severity": "fail", "check": "walker: defaultValue is absent or a string",
      "statement": "`defaultValue` is a mustache-template STRING, never a literal array/number/object — the resolver calls `.match()` on it and a non-string throws `e.match is not a function`, killing the render. Multi-select defaults are bound through form data, never a literal array." },
    { "id": "R-010", "group": "data", "severity": "fail", "check": "walker: inline column editComponent/createComponent is [not-editable] or {type, settings:{...}} — never [default], never a flat model",
      "statement": "Inline-editing column editors MUST be `{ \"type\": \"[not-editable]\" }` or `{ \"type\": \"<editor>\", \"settings\": { full component model with its own type+version+editMode+hideLabel } }`. `[default]` is only valid on displayComponent; a flat model without `settings` throws `reading 'version'`. Per-row CRUD needs a `columnType: \"crud-operations\"` column plus canEditInline/canAddInline/canDeleteInline: \"yes\"." },
    { "id": "R-011", "group": "data", "severity": "fail", "check": "walker: checkboxGroup with dataSourceType 'values' uses items[] of {label,value}",
      "statement": "`checkboxGroup` hardcoded options use `items` (NOT `values`), each `{label, value}`, with dataSourceType \"values\". dropdown/radio use `values` with `{id,label,value}` — do not conflate." },
    { "id": "R-012", "group": "scripts", "severity": "fail", "check": "validate-guardrails.js walker: a code-mode prop (hidden/disabled/customEnabled/editMode/…) holding a code-looking raw string instead of {_mode:'code',_code}",
      "statement": "Code-mode props are objects `{ \"_mode\": \"code\", \"_code\": \"...\" }` — a code-carrying prop stored as a plain JS string is silently stripped on save." },
    { "id": "R-013", "group": "scripts", "severity": "fail", "check": "validate-guardrails.js walker: every script-carrying string compiles as an async-function body (syntax error or smart quote = fail, template literal = warn)",
      "statement": "All embedded script strings must be JSON-safe and parseable: they compile as a JS (async) function body, carry no smart quotes and no unescaped newlines. A broken script string breaks the outer JSON with browser-side parse errors. Template literals parse but are the usual source of the breakage — prefer string concatenation." },
    { "id": "R-014", "group": "binding", "severity": "fail", "check": "validate-guardrails.js walker: single-brace {expr} in template-carrying string props (script/JSON-carrying props excluded)",
      "statement": "Mustache expressions always use {{double braces}}. Single-brace expressions are silently ignored, producing empty values with no error." },
    { "id": "R-015", "group": "binding", "severity": "fail", "check": "resolve-bindings: referenceListId matches the property's referenceListName/Module from live metadata; list exists with ≥1 item",
      "statement": "A reference list's identity is READ from metadata, never guessed — `referenceListId.{module,name}` copied verbatim from the property's referenceListName/referenceListModule. A derived name renders a silently EMPTY dropdown." },
    { "id": "R-016", "group": "binding", "severity": "fail", "check": "resolve-bindings: formSettings.modelType is the {name,module} object resolved from live EntityConfig; dataContext.entityType is the fullClassName string",
      "statement": "On 0.45 `formSettings.modelType` is the `{ name, module }` OBJECT resolved from the live EntityConfig (never assumed); `dataContext.entityType` stays the fullClassName STRING. A namespace mismatch 500s at runtime." },
    { "id": "R-017", "group": "data", "severity": "warn", "check": "formSettings: entity-bound forms use gql loader/submitter unless a custom endpoint was explicitly requested",
      "statement": "Entity-bound forms use the default `dataLoaderType/dataSubmitterType: \"gql\"`. Custom form-level endpoints are opt-in only — wired solely on explicit request and built via shesha-app-layer first." },
    { "id": "R-018", "group": "structure", "severity": "warn", "check": null,
      "statement": "`editMode` is per form type — never blanket-stamp. Detail forms with Start Edit/Submit lifecycle: \"inherited\". Create/edit dialogs and anonymous pages: \"editable\". Read-only rails/KIB strips: \"readOnly\" (\"inherited\" renders BLANK in the default view state). Visual components: omit." },
    { "id": "R-019", "group": "process", "severity": "process", "check": null,
      "statement": "\"list\"/\"cards\"/\"feed\"/\"tiles\" → `datalist`; \"table\"/\"grid\"/\"spreadsheet\" → `datatable`. Honor the user's noun; multi-select-from-a-list = selectionMode \"multiple\" on the datalist. Genuinely ambiguous → ask." },
    { "id": "R-020", "group": "structure", "severity": "warn", "check": null,
      "statement": "Minimal component count: the requested fields + validationErrors + ONE buttonGroup with Submit AND an exit button + minimum layout structure. The Submit/exit pair is part of the floor even for terse prompts; padding (extra containers, decorative panels, unrequested toolbar chrome) is a defect." },
    { "id": "R-021", "group": "structure", "severity": "warn", "check": "walker: input components carry a label that differs from raw propertyName",
      "statement": "Human-readable labels on every field — labels are user-facing AND how browser tests locate fields." },
    { "id": "R-022", "group": "security", "severity": "fail", "check": "validate-guardrails.js: a form whose name/label reads as anonymous (login/register/otp/forgot/reset/public) carries formSettings.access 5",
      "statement": "`access: 5` on anonymous forms (login, register, OTP); verify post-push by re-fetch." },
    { "id": "R-023", "group": "scripts", "severity": "warn", "check": "grep: globalState writes in scripts",
      "statement": "No `globalState` for cross-form state — use `contexts.appContext` (app-wide) or `pageContext` (inter-page). localStorage/sessionStorage only for hard-refresh-surviving, non-sensitive state." },
    { "id": "R-024", "group": "scripts", "severity": "warn", "check": "grep: .then( chains / missing try-catch around http calls in scripts",
      "statement": "API calls in embedded scripts use try/catch + async/await, never .then() chains." },
    { "id": "R-025", "group": "structure", "severity": "fail", "check": "validate-guardrails.js --baseline <fetched.json>: every component id present in the baseline still exists in the new markup (skipped without a baseline)",
      "statement": "Preserve ids on existing components when editing — fresh GUIDs only on clones/new nodes." },
    { "id": "R-026", "group": "api", "severity": "process", "check": null,
      "statement": "API namespace is per service, never guessed: FormConfiguration under /api/services/Shesha/…; Metadata, Module, EntityConfig, ReferenceList under /api/services/app/…. A 404 usually means wrong namespace, not a missing resource." },
    { "id": "R-027", "group": "api", "severity": "process", "check": null,
      "statement": "UTF-8, BOM-free bytes for the cached token and every staged JSON file — a BOM poisons `Authorization: Bearer` and Node JSON.parse." },
    { "id": "R-028", "group": "styling", "severity": "fail", "check": "validate-guardrails.js walker: no root-level `columns` component (page-level split); no customStyle flex / style-channel flexShrink; nested `columns` warns",
      "statement": "Layout splits are flex-container children sized via `desktop.dimensions.width` (accepts % and calc()) — NEVER the Shesha `columns` component, never customStyle:{flex} (ignored), never style-channel flexShrink (never reaches the outer div)." },
    { "id": "R-029", "group": "styling", "severity": "fail", "check": "walker: a container with flex intent sets desktop.display 'flex' (the flex model lives in the desktop breakpoint block, not root)",
      "statement": "A flex container MUST carry its flex model in the `desktop` breakpoint block — `desktop.{display:'flex', flexDirection, justifyContent, alignItems, gap, dimensions}`. The 0.45 renderer reads layout from `desktop.*`, NOT from root-level props; root-only display/flexDirection are inert and children stack (proven live). Sizing a split child = `desktop.dimensions.width` (+ `minWidth:'0px'` so flex items shrink instead of wrapping)." },
    { "id": "R-030", "group": "styling", "severity": "process", "check": null,
      "statement": "Appearance goes through desktop/tablet/mobile breakpoint blocks (the measured channels in measured-capability-matrix.json). background/shadow render only as COMPLETE objects (background needs `type`; partial sub-path writes are dead — gym-measured). The legacy `style` JS-string renders inline and WINS over everything — when a stamped prop doesn't render, grep the component and its ancestors for a truthy `style` first." },
    { "id": "R-031", "group": "structure", "severity": "fail", "check": "walker: customVisibility absent; conditional visibility via code-mode hidden",
      "statement": "Conditional visibility = code-mode `hidden` ({_mode:\"code\",_code:\"return !(data?.x)\"} — TRUE hides). Legacy `customVisibility` is IGNORED on 0.45. Use optional chaining: create forms have no data context initially and a throw fails-open." },
    { "id": "R-032", "group": "styling", "severity": "process", "check": null,
      "statement": "A container renders TWO divs: the outer (sha-components-container, the flex item) receives only dimensions+shadow; layout props land on the inner. Inner overflow is hard-coded `overflow:auto` — the model's overflow prop is a no-op; fix squeezed headers with `dimensions.minHeight: \"fit-content\"`." },
    { "id": "R-033", "group": "styling", "severity": "process", "check": null,
      "statement": "Field-level `labelCol` is ignored — only formSettings.labelCol/wrapperCol applies (field-level labelAlign IS honored). `.sha-page-content:not(.no-padding)` carries a hard-coded 12px inset." },
    { "id": "R-034", "group": "binding", "severity": "fail", "check": "resolve-bindings: every bound propertyName exists on the modelType (live metadata), dotted paths resolved segment-wise",
      "statement": "A bound value renders ONLY when `propertyName` is a real entity property — it drives the gql fetch. Mutating data in onAfterDataLoad or form.setFieldsValue does NOT populate a read-only field's display." },
    { "id": "R-035", "group": "binding", "severity": "warn", "check": null,
      "statement": "Mustache {{ }} HTML-escapes ' & < > — use {{{triple-brace}}} for trusted display values. GUIDs are unaffected." },
    { "id": "R-036", "group": "styling", "severity": "process", "check": null,
      "statement": "`refListStatus` fill colour comes ONLY from the reference-list item's own colour — no item colour = grey regardless of solidBackground. Radius via desktop.border.radius.all, never customStyle." },
    { "id": "R-037", "group": "data", "severity": "fail", "check": "validate-guardrails.js (+ entity metadata as arg 2): a submitting form binding an FK-object property must carry formSettings.onPrepareSubmitData that reduces it; WARN without metadata (FK-ness is read from metadata, never guessed)",
      "statement": "Dynamic CRUD Update rejects an FK sent as a nested object (\"not allowed to be updated\") — reduce every FK to `{ id }` in formSettings.onPrepareSubmitData." },
    { "id": "R-038", "group": "scripts", "severity": "process", "check": null,
      "statement": "Execute Script actions must return a Promise (no IIFEs); `http.get(url,{params})` drops params — query args go in the URL string; formArguments/selectedRows are NOT in Execute Script scope (multiselect via the context's selectedIds)." },
    { "id": "R-039", "group": "data", "severity": "process", "check": null,
      "statement": "0.45 lifecycle: `onInitialized` is NOT wired on dynamic pages — custom data loading goes through `onAfterDataLoad` (async-capable). `dataLoaderType: \"custom\"` is silently skipped whenever the page supplies initialValues (DynamicPage always does)." },
    { "id": "R-040", "group": "process", "severity": "process", "check": null,
      "statement": "Plan ALL backend changes up front and apply in ONE build + double-boot: a NEW entity needs TWO boots (its CRUD controller registers on the boot after EntityConfig seeds); reflist items + app-service code need one. Verify entity CRUD Create returns 200 BEFORE authoring the form." },
    { "id": "R-041", "group": "security", "severity": "fail", "check": "validate-guardrails.js: a form declaring formSettings.access 5 must not carry a shesha.form Submit over the default gql (raw entity CRUD) submitter",
      "statement": "NEVER expose raw entity CRUD to the anonymous internet. Public-form writes go through a custom [AbpAllowAnonymous] app service that forces server-side values and re-enforces validation; the form Submit posts to it via Execute Script, then Navigates." },
    { "id": "R-042", "group": "process", "severity": "process", "check": "validate-styledness.js exit code",
      "statement": "No form ships unstyled. A structurally-complete default-grey form is a defect — every no-design build ends with the shipped `shesha` theme applied via shesha-design-system before push (skipped only by explicit --no-style)." },
    { "id": "R-043", "group": "actions", "severity": "warn", "check": null,
      "statement": "CRUD wiring is canonical: table Add = buttonGroup item with Show Dialog (modal, formId {name,module}, modalWidth 60%, formMode edit) — never Navigate; detail lifecycle = Start Edit / Submit / Cancel Edit (owner shesha.form); standalone create/edit page = Save (Submit, primary) + Back (Navigate); toolbar Refresh/Toggle Columns use the dataContext component's id as actionOwner; row→detail = action column navigate to /dynamic/<module>/<form>?id={{selectedRow.id}}." },
    { "id": "R-044", "group": "actions", "severity": "fail", "check": "walker: no actionName 'Delete row' with owner 'table'",
      "statement": "Row delete/unlink = Execute Script + `await http.delete(...)` + onSuccess Refresh table with actionOwner = the dataContext component id. `Delete row`/owner \"table\" does not exist and throws." },
    { "id": "R-045", "group": "data", "severity": "warn", "check": null,
      "note": "Not mechanically checkable from markup: WHICH FKs are contextually preset is a property of the calling screen, not of this form. R-037 covers the checkable half (a submitting form that binds an FK-object property needs an onPrepareSubmitData reducer).",
      "statement": "Contextually-preset required FKs on create dialogs need BOTH a real bound component AND formSettings.onPrepareSubmitData — formArguments/setFieldsValue alone never reach the submit payload. Omission = Crud/Create 500." },
    { "id": "R-046", "group": "process", "severity": "process", "check": "Stop hook: push-ledger entries all verified/abandoned",
      "statement": "A validated file on disk is NOT a delivered form. Every authored form is pushed to the backend and verified (re-fetch + diff) before the task is reported done — enforced by the push-ledger Stop hook." },
    { "id": "R-047", "group": "process", "severity": "process", "check": null,
      "statement": "Verification = re-fetch the pushed form and diff against what was sent; the 200 alone proves nothing. Browser checks clear the form cache [R-056] and navigate table→details (never a hand-built ?id= URL)." },
    { "id": "R-048", "group": "styling", "severity": "process", "check": null,
      "statement": "Datalist row-template cards: name-mode bound text, `dimensions: fit-content`, single-line ellipsis for long text, status chip on its own row, padding/overflow via the legacy `style` prop, card height \"auto\" — the collapse/overflow fix lives in the block subtree." },
    { "id": "R-049", "group": "versioning", "severity": "process", "check": "backend-probe.mjs --versions harvests {componentType: version} from live forms → validate-guardrails.js --live-versions <file> FAILs on drift; without it the KB comparison only WARNs (R-003)",
      "statement": "Component versions are framework-release-specific and DRIFT across point releases. The 0.45 components-kb (_index.json) is authoritative for this plugin; when exactness matters probe a live form on the target backend (`backend-probe.mjs --versions`) and gate against the harvested map. Version facts are the only cross-generation knowledge kept here — 0.43 authoring lives in the shesha-developer-0-43 plugin." },
    { "id": "R-050", "group": "process", "severity": "process", "check": null,
      "statement": "Never read a whole large seed/golden form — grep the fragment you need. Whole-file reads of 100KB+ markup are the top context-burn failure." },
    { "id": "R-051", "group": "styling", "severity": "warn", "check": null,
      "statement": "0.45 app-level theme delivery has no `token`/`components` channel: IConfigurableTheme carries only primaryColor/errorColor/warningColor/successColor/infoColor + layout/text roles; `processingColor`, `text.link`, and any antd `token`/`components` block are silently dropped on write. Deliver such styling per-component at compile time instead." },
    { "id": "R-052", "group": "styling", "severity": "fail", "check": "validate-guardrails.js walker: a text/paragraph/title node authoring a font colour must carry contentType:\"custom\"",
      "statement": "A `text` component's font colour renders ONLY with `contentType: \"custom\"` — antd's own contentType presets otherwise win and `desktop.font.color` is measured as a pure no-op. Every text/heading node that authors a colour sets `contentType: \"custom\"`; `font.size`/`font.weight` apply regardless. Omission is invisible in the markup and ships illegible text (light ink on a dark band renders near-black)." },
    { "id": "R-053", "group": "styling", "severity": "fail", "check": "validate-guardrails.js: authored breakpoint-block appearance channels are cross-checked against assets/measured-capability-matrix.json; a channel whose every measured variant is `no-op` fails",
      "statement": "Never author a channel the capability matrix measured as a no-op — a dead prop is a defect, not decoration: the design intent silently never reaches the DOM and the next reader believes the form is styled. Author only channels measured as changes-style / changes-geometry / renders for that component type, or reach the effect through the compound that does work (e.g. text colour via contentType:\"custom\" [R-052])." },
    { "id": "R-054", "group": "styling", "severity": "fail", "check": "validate-guardrails.js walker: a breakpoint background of type \"image\" carrying only a url (no storedFile/uploadFile) fails",
      "statement": "A container `background.type: \"image\"` with a plain `url` renders `url(null)` — the renderer resolves background images from stored-file paths only, never from an arbitrary URL. Show a picture with an `image` component sized via `desktop.dimensions` instead of a container background." },
    { "id": "R-055", "group": "styling", "severity": "fail", "check": "validate-guardrails.js walker: an image node carrying position:absolute in any style channel fails",
      "statement": "An `image` component renders inside an UNSIZED antd `ant-image` wrapper div, so taking the inner img out of flow collapses it to 0×0 — never `position: absolute` on an image. Size it via `desktop.dimensions` (width/height) and keep it in flow; the flat `width`/`height`/`objectFit`/`objectPosition` props are measured no-ops [R-053]." },
    { "id": "R-056", "group": "process", "severity": "process", "check": null,
      "statement": "After ANY push the adminportal's IndexedDB caches (`form`, `form_lookup`) keep serving the previous markup — clear both from a non-app page (e.g. `<FRONTEND_URL>/favicon.ico`; an in-app `deleteDatabase` blocks silently) before every browser verification, or you measure a ghost of the last build. Canonical recipe: references/verification.md §2." },
    { "id": "R-057", "group": "styling", "severity": "fail", "check": "validate-guardrails.js walker: a buttonGroup with ≥2 button items carries isInline:true; a container whose direct children include ≥2 button/buttonGroup components carries desktop.display flex + desktop.flexDirection row (a vertical stack of action buttons fails)",
      "statement": "2+ adjacent action buttons render INLINE — a `buttonGroup` carries `isInline: true` (without it the whole group collapses to an overflow \"…\" menu), and separate button components sharing a container sit in a real flex row (`desktop.display: \"flex\"` + `desktop.flexDirection: \"row\"`), never stacked one per line. In a capture footer that row is right-aligned via `justifyContent: \"flex-end\"` on the CONTAINER — a buttonGroup's own dimensions/stylingBox are measured no-ops, so it has no alignment lever of its own." }
  ]
}
