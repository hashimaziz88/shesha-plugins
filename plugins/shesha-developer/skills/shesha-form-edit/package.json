{
  "name": "shesha-form-edit",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.*js\" && node scripts/check-doc-links.mjs",
    "test:contract": "node --test \"tests/contract/*.contract.test.mjs\"",
    "setup": "npm install && npx playwright install chromium --with-deps"
  },
  "devDependencies": {
    "playwright": "^1.40.0"
  },
  "dependencies": {
    "ajv": "^8.20.0"
  }
}
