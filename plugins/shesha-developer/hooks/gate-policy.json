{
  "version": 1,
  "measuredAgainst": "935-form production corpus (RequirementsStudio, MembershipManagement, AssetManagement2, UtilityManagement) + 15 bundled seeds — see skills/shesha-form-edit/docs/corpus-report.md",
  "notes": [
    "This policy gates a CURATED SUBSET of Tier 1/2 codes, not every Tier 1/2 finding. A blanket gate was measured and rejected — see corpus-report.md's go/no-go section: T1-PROP-UNKNOWN fires on 93.9% of real forms and 100% of this plugin's own bundled seeds.",
    "validate-push.mjs runs normalize() BEFORE tier1/tier2, so a Group B finding present in the validator's output already survived normalization — no separate before/after comparison is needed.",
    "Rates are 'forms affected' percentages from the post-fix full-corpus table in corpus-report.md, except where marked 'seed-only' (no corpus rate existed at that granularity).",
    "Edit this file to change what blocks a push. No code change is required to narrow, widen, or re-triage a code — that is the point of keeping the policy in data.",
    "Task 8 added six codes derived from a forensic analysis of two real failed form builds (flight-*/asset-* forms) and re-graded the full 935-form corpus with them in place — see corpus-report.md's task-8 section. T2-WIDTH-ON-NONCONTAINER was narrowed at the same time (it no longer counts a proportional width — that's T2-SPLIT-WIDTH-ON-LEAF's exclusive territory now — or a literal \"100%\", which is the exact value the split-width fix stamps onto a leaf); its rate below is the POST-narrowing measurement (13.3%, was 19.9%)."
  ],
  "groups": {
    "A": {
      "action": "block",
      "label": "Group A — BLOCK",
      "description": "High-confidence, low-noise renderability/contract breaks that the normalizer cannot repair. Includes the render-crash family (32 crash signatures recorded in a 2-day telemetry window) plus low-rate structural contract breaks.",
      "codes": {
        "T1-JSON-UNSAFE": { "rate": 0.155, "justification": "A template literal or raw newline in a script/endpoint string breaks the outer JSON.stringify on push. 15.5% of the corpus, 318 instances — genuine defect, not normalizer-fixable." },
        "T1-SCRIPT-SYNTAX": { "rate": 0.022, "justification": "A broken embedded script string throws a JSON parse error in the browser. Low rate (2.2%), unambiguous when it fires." },
        "T1-DEFAULTVALUE-NONSTRING": { "rate": 0.052, "justification": "The resolver calls .match() on defaultValue; a literal array/number/object throws. 5.2% of the corpus." },
        "T1-EDITCOMPONENT-SHAPE": { "rate": 0.002, "justification": "A malformed datatable editComponent/createComponent throws 'reading migrator'/'reading version'. Rarest Group A code (0.2%) but a hard crash when it fires." },
        "T1-TYPE-UNKNOWN": { "rate": 0.039, "justification": "An unregistered component type renders nothing. Rate measured post-fix (after the items[]-pseudo-node leak into this code was corrected in Task 5) — 3.9%, down from 13.5% pre-fix." },
        "T1-DOUBLE-SLOT": { "rate": 0.065, "justification": "A card/collapsiblePanel with children in both content.components AND components[] renders its body twice, often with colliding ids. 6.5% of the corpus." },
        "T2-VALIDATIONERRORS-MISSING": { "rate": 0.154, "justification": "Missing validationErrors wiring is a structural contract break the normalizer has no mechanical fix for. 15.4% of the corpus." },
        "T2-SUBMIT-WIRING": { "rate": 0.019, "justification": "A submit button not wired to the expected action silently fails to save. Low rate (1.9%)." },
        "T2-EXIT-MISSING": { "rate": 0.001, "justification": "No exit/cancel wiring on a dialog/lifecycle form. Rarest measured code (0.1%, 1 instance) but a genuine dead-end when it fires." },
        "T2-DATACONTEXT-PROPS": { "rate": 0.004, "justification": "A dataContext node missing a genuinely required prop, measured post-fix (the false-positive uniqueStateId requirement was removed in Task 5). 0.4% of the corpus." },
        "T2-DATE-COMPONENT": { "rate": 0.004, "justification": "Date component contract break. Low rate (0.4%, 5 instances)." },
        "T2-CODEMODE-TITLE": { "rate": 0.012, "justification": "Task 8. 1.2% of the corpus (11/935 forms, 23 instances). A content._mode:\"code\" block that string-concatenates data fields collapses to its bare separator literal (byte-for-byte, e.g. \" · \", \"() → ()\") the instant any referenced field is unresolved, with no thrown error — reproduced verbatim against flight-details.pushed.json's real broken output. Not normalizer-fixable: rewriting the code into an equivalent mustache template requires knowing the author's intended separator/punctuation, which is a judgment call a mechanical transform must not guess at. Low rate, unambiguous predicate (requires both a data-field reference AND string concatenation) — blocked rather than report-only." },
        "T2-DUPLICATE-CAPTION": { "rate": 0.009, "justification": "Task 8. 0.9% of the corpus (8/935 forms, 10 instances). A standalone text node duplicating a hideLabel sibling's own label — the caption authored twice, reproduced verbatim against asset-detail.pushed.json's railStatusPanel. Not normalizer-fixable: which half to delete (the text node, or the sibling's hidden label) is an authoring decision, not inferable from the markup alone. Low rate, unambiguous when it fires." },
        "T2-LABELCOL-VS-NARROW-ROW": { "rate": 0.018, "justification": "Task 8. 1.8% of the corpus (17/935 forms, 17 instances), 6.7% of bundled seeds (rs-detail-with-header.json). formSettings.layout:\"horizontal\" + a global labelCol span applied to a field sitting in a sub-50%-width container truncates/crams the label — reproduced verbatim against asset-detail.pushed.json (\"Assign Employ...\", cramped \"Asset Name :\"). Not normalizer-fixable: the repair (switch to vertical layout, or resize every labelCol/wrapperCol span) is a layout-authoring decision this task's normalizer must not make unilaterally. Low rate, single well-evidenced root cause." }
      }
    },
    "B": {
      "action": "normalize-then-block",
      "label": "Group B — NORMALIZE THEN BLOCK",
      "description": "normalize-form.mjs mechanically repairs these. Because validate-push.mjs always normalizes before validating, a finding in this group appearing in the validator's output means the normalizer already ran and failed to fix it — that is what makes blocking here safe.",
      "codes": {
        "T1-ID-DUPLICATE": { "rate": 0.114, "justification": "normalize-form.mjs Phase B (fixId) mints a deterministic replacement id for any duplicate. Surviving this means the normalizer itself threw or was skipped." },
        "T1-PARENT-MISSING": { "rate": 0.332, "justification": "normalize-form.mjs Phase B stamps parentId from the walked tree structure (\"root\" at top level, else the parent's id) on every pass." },
        "T1-VERSION-MISSING": { "rate": 0.274, "justification": "normalize-form.mjs Phase B stamps version from the registry for any type with a non-null registry version." },
        "T2-WIDTH-ON-NONCONTAINER": { "rate": 0.133, "justification": "Task 8: rate re-measured at 13.3% (was 19.9%) after narrowing this code to exclude proportional widths (now T2-SPLIT-WIDTH-ON-LEAF's exclusive territory) and the literal \"100%\" (the exact value that check's fix stamps onto a leaf — flagging it here would mean the two checks' fixed points could never both be satisfied at once). normalize-form.mjs Phase A5 still strips dimensions.width from non-container nodes unconditionally (after relocating it onto a wrapper where needed in A3)." },
        "T2-FLEXCHILD-NOT-CONTAINER": { "rate": 0.125, "justification": "normalize-form.mjs Phase A3 wraps a bare non-container child of a flex-row container." },
        "T2-COLUMNS-PRESENT": { "rate": 0.233, "justification": "normalize-form.mjs Phase A2 converts a columns node into a flex container + one child container per column." },
        "T2-FLEX-NO-DISPLAY": { "rate": 0.624, "justification": "normalize-form.mjs Phase A6 sets display:\"flex\" wherever a flex-only prop (gap/flexDirection/etc) is set without it. Highest pre-gate rate in Group B (62.4%), which is exactly why it must go through normalization first rather than being a Group A block." },
        "T2-SPLIT-WIDTH-ON-LEAF": { "rate": 0.004, "justification": "Task 8. 0.4% of the corpus (4/935 forms, 12 instances) — but 62/69 inputs across the four real flight-* forms this check was derived from (measured directly, outside the corpus: those forms predate/postdate the 935-form historical dump). normalize-form.mjs's new wrapSplitWidthLeaves step (independent of the pre-existing isFlexRowNode/A3 flex-row-only detection, which real corpus containers were found to defeat by carrying flex props only nested under desktop/tablet/mobile with no top-level mirror) wraps the leaf in a new container carrying the proportional width and clears the finding to 0/12 instances corpus-wide, and 0/12 on the four real evidence forms directly." },
        "T2-SLOT-STYLE-MISMATCH": { "rate": 0.0, "justification": "Task 8. 0/935 corpus forms and 0/15 bundled seeds — this exact bug pattern (a card/collapsiblePanel styling itself but not its content/header/customHeader slot) does not appear in the historically-graded corpus. Measured directly instead against the real evidence that motivated this check: flight-details.pushed.json and flight-booking-details.pushed.json each carry 2 instances (statusPanel + metaPanel), reproduced as the literal run-on strings seen on screen (\"StatusFlight status\", \"RecordCapturedUpdated\"). normalize-form.mjs's new propagateSlotStyle step clears all 4 instances to 0. Placed in Group B on the strength of that direct clearing proof and the check's inherent low noise (>=2 slot children required), despite the corpus showing 0% — a rate this low is the safest possible Group B candidate, not a reason to withhold it." },
        "T2-ROWLIST-NO-VGAP": { "rate": 0.037, "justification": "Task 8. 3.7% of the corpus (35/935 forms, 52 instances), 6.7% of bundled seeds (rs-detail-with-header.json). A components array whose direct children are 2+ flex-row containers needs its OWN vertical gap or row spacing falls back to each row's intrinsic content height — reproduced against flight-details.pushed.json's three field tabs. normalize-form.mjs's new normalizeRowListGap/normalizeTabRowListGap steps (stamp a gap directly on a real container host; wrap a tab pane's rows in a new gap-bearing container when the pane itself has no style props at all in the registry) clear all 52 instances to 0 corpus-wide." }
      }
    },
    "C": {
      "action": "report-only",
      "label": "Group C — REPORT ONLY, never blocks",
      "description": "Everything else: every Tier 3 code (observe-only by design, never enforced per validate-form.mjs), plus the Tier 1/2 codes measured too noisy or too unproven to gate today. These still appear in validate-push.mjs's reported findings and in the log, so they stay visible without being able to halt a push.",
      "codes": {
        "T1-PROP-UNKNOWN": { "rate": 0.939, "justification": "93.9% of the corpus (24,114 instances) even after fixing the confirmed common-style-schema false-positive prefix in Task 5. A residual, lower-confidence long tail remains — see corpus-report.md. Fires on 100% of this plugin's own bundled seeds; gating it would block the skill's own recommended starting point." },
        "T1-VERSION-STALE": { "rate": 0.839, "justification": "83.9% corpus-wide, but the per-DB split is essential: 25% on RequirementsStudio (registry version matches project version) vs ~93% on the two older-schema DBs (AssetManagement2/UtilityManagement) — a corpus/registry VERSION-MISMATCH artifact, not evidence of genuine staleness, on those two. Needs per-project registry pinning before it can gate safely." },
        "T2-STYLE-INCOMPLETE": { "rate": 0.690, "justification": "69.0% of the corpus; fires on 100% of this plugin's own bundled seeds (rs-detail-with-header.json alone carries 435 Tier-2 findings for this code). Genuine defect, but far too high a rate to gate without first remediating the seeds." },
        "T2-EDITMODE-MISMATCH": { "rate": 0.557, "justification": "55.7% post-fix (readOnly exemption applied in Task 5). Still the single highest-rate Tier 2 code after the fix; not yet proven safe to gate." },
        "T2-PROPERTYNAME-CASE": { "rate": 0.365, "justification": "36.5% post-fix (dotted-path segment fix applied in Task 5). Fires on 60% of bundled seeds." },
        "T2-LOOSE-BUTTON": { "rate": 0.302, "justification": "30.2% of the corpus, matches the brief's own measured baseline on the RS-100 cohort. Genuine defect, high rate." },
        "T2-MODELTYPE-SHAPE": { "rate": 0.218, "justification": "21.8% post-fix (down from 99.3% before the {name,module}-vs-string shape fix in Task 5). Still too fresh a fix to gate without more corpus cycles." },
        "T2-DROPDOWN-SOURCE": { "rate": 0.240, "justification": "24.0% post-fix (entityType shape fix applied in Task 5)." },
        "T2-STYLE-OFF-TOKEN": { "rate": 0.091, "justification": "9.1% post-fix (framework-default-color exemption applied in Task 5). Under 10% but grouped with its Tier 3 sibling (T3-RAW-HEX) which is not similarly fixable — kept report-only for consistency until proven independently safe to gate." },
        "T3-*": { "rate": null, "justification": "Every Tier 3 code is observe-only by design (validate-form.mjs's own rule: only Tier 1/2 findings can set exitCode=1). Never gated, regardless of hit rate." }
      }
    }
  }
}
