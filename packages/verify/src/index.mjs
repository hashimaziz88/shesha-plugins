// @shesha/verify — L3: the T1-T5 verifier tiers, every repository gate, the
// mutation harness and the gate evidence writer (D-002).

export * from './coverage.mjs';

/** Verifier API version. */
export const VERIFY_API_VERSION = '0.1.0';
