=== SHESHA SFS REBUILD — INTEGRATION PROOF (SCOPE B) ===
scope            WP-5c WP-5d WP-5e WP-2b WP-3b.1 WP-3b.2 WP-3b.3 WP-3b.3b WP-3b.3c WP-3b.4 WP-1c WP-6 WP-9 WP-7 WP-8a WP-8b.1 WP-8b.2 WP-8b.3 WP-8c.1 WP-8c.2 WP-8d WP-3c WP-3d   23/23 complete
compiler robust  leaf input types compile: checkbox, switch, radio (was the 498-form compile-npe)
decompiler hygiene hostile header lifts clean: form=my-form-v2-draft, entity omitted, label non-empty (SFS-1101 /form /entity /label)
columns lift     equal columns grid lifts to a flex row (was the #1 IR escape, 241 forms)
full registry    registry 93/121 components full (>= 93, was 12) and deferredAuthorable 7 (< 8): the settings-form extractor types ~80 beyond the 13 priority anchors, framework-verified overlay props count as full, paragraph is legacy (BL-004/020/022, D-113)
handoff schemas  seven handoff schemas compile under ajv strict; the plan schema forbids a prose assertion, < 3 predicates, a non-T3 contract and > 3 repair rounds (WP-8a, D-116)
hook contract    block-form-writes denies a .form.json write (HOOK-0101), allows a .sfs.json write, fails closed on no repo root (HOOK-0001); exit(1) banned (WP-8b.1, D-119)
sfs validate CLI sfs validate exits 0 on a schema-valid lock and 1 on one missing `role`; enforce-screen-lock denies a lockless screen write (HOOK-0402), validate-sfs-on-write blocks when the validator cannot start (HOOK-0002) (WP-8b.2, D-120)
push admission   push admission is one program (P0–P9): T1–T3 pushAdmissible admits green and a --allow-partial backend outage, refuses otherwise; admit refuses a missing artifact (HOOK-0302) and an undeterminable push (HOOK-0301); gate-push maps the refusal, gate-dispatch enforces judge isolation (HOOK-0503), session-start prints six lines and never blocks (WP-8b.3, D-121)
markup provenance INV 1 has a real enforcer: g-markup-provenance recompiles 16 committed artifacts byte-identically from their sibling SFS (closes D-003), g-specwriter-purity keeps specwriter logs free of the six markup fingerprints, and the prop-group index moved to packages/registry/data/prop-groups as clean-form-config was deleted (WP-8c.2, D-124/D-125)
mcp tool surface the shesha-sfs MCP surface is seven tools behind one implementation: registry_lookup resolves datatable to v29 exactly, g-mcp-cli-parity locks the tool set to the CLI subcommand set (7 == 7), and /.mcp.json launches the stdio server (WP-8d, D-126)
strict index     noUncheckedIndexedAccess: true — every indexed access is checked (BL-010, was D-024 deferral)
prose thin       design skills thinned; no BL-007/BL-012 prose waiver remains
placement sidecar sidecar carries placement: pageShell region=page, a fill cell reserves 216px, 23 nodes complete (§3.3.1)
placement predicates placement predicates evaluate: 4 contract rows pass over the sidecar, an absent node fails (D-014, the 18-name engine)
T3 semantic tier T3 offline tier passes clean and catches a defect: 18 checks, a wrong dataContext entityType flips it to fail (§3.2.4, D-106)
placement contract placement contract evaluates: 8 rows + a column set pass over the compiled screen, a drifted row flips it to fail (T3.20/21/22, D-107)
T3 metadata substrate T3 backend checks resolve with a recorded snapshot (pass) and degrade to uninspectable without it (partial), never a pass (§3.2.4, D-108; D-036 closed)
check-references lift g-check-references, lifted onto the coverage API, passes over the cleaned plugin; the quarantined husks are gone (D-049 closed, D-109)
precedent index  precedent Index A retrieves 3 shapes over 11 corpus forms (method=shape, no E_NOT_IMPLEMENTED); g-rag-isolation holds (BL-009, D-110)
corpus round-trip corpus round-trip: 7 of 12 forms clean+stable at rate 1.00 (>= 0.90, was 4); 1 documented escape (BL-021), 4 await BL-024 container node-types (sectionSeparator, collapsiblePanel, tabs); the datatable-column + action-grammar lift closed GAP-001 (BL-002, D-111)
T4/T4b residue   T4b over a probe recorded from a live Shesha screen (98 nodes, 17 carrying data-sha-c-name) passes; the overflow probe fails and the name-stripped probe is uninspectable, never a pass; T4's selftest passes over the stub backend and T4 states what is missing rather than running (WP-3c, D-127)
T5 advisory      T5 ranks on 5 orthogonal axes with no total anywhere and exits 0 on every grade (excellent->excellent acceptable->acceptable generic->generic broken->broken disqualified->notRun); result is unchanged whether or not T5 runs, and g-t5-advisory holds: t5 references in gate-push.mjs: 0 · t5 references in result computation: 0 · judge-truth gap recorded for 1.11.0 (WP-3d, D-128; D-015 closed after 11 days pending)
