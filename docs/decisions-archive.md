## Archived decisions

| ID | Date | Status | Decision | Why | Consequence | Enforced by | Confirmation |
|---|---|---|---|---|---|---|---|
| D-002 | 2026-08-17 | decided | The toolchain lives in this repo as npm workspace packages under `packages/` | §9 q4 | One `npm ci`; skills hold no logic | g-workspace-hygiene | n/a |
| D-005 | 2026-08-17 | decided | Coverage accounting exists once in `packages/registry/src/coverage.mjs`; family sets are module constants; `walkComponents` lives once in `packages/verify/src/walk.mjs` | §1.4 five walkers, three unwrappers | Two implementations cannot disagree | g-coverage-single-impl | n/a |
| D-006 | 2026-08-17 | decided | Zero coverage is a hard fail: `walked === 0` fails, `walked > 0 && checked === 0` fails, any `uninspectable` yields exit 3 `partial` | §1.4 | No green over unvisited territory | structural:packages/registry/src/coverage.mjs | n/a |
| D-007 | 2026-08-17 | decided | Every gate exports `mutations[]` with >= 2 entries and `gates:mutate` proves each flips the verdict | §5 | Stub gates are unrepresentable | g-gate-contract | n/a |
| D-009 | 2026-08-17 | decided | Contradictions are resolved by deleting the losing side, never by adding a note explaining which side wins | §1.2#2 | No "which wins" annotations survive | g-prose-budget | n/a |
| D-012 | 2026-08-17 | decided | `summarize.js` and both of its invocations are deleted; no documented command in the repo is unexecutable | §1.4 | Every documented command resolves | g-commands-executable | n/a |
| D-020 | 2026-08-17 | decided | One commit per work package; the numeric record of a commit is `packages/verify/evidence/<WP>.json`, written by the gate runner | §1.4, §5 | No commit-body number is hand-typed | g-commit-format | n/a |
| D-023 | 2026-08-17 | decided | No TypeScript build step; types are JSDoc checked by `tsc --noEmit` with `checkJs` and `strict` | model-agnostic constraint | Every program runs under bare node | g-workspace-hygiene | n/a |
| D-024 | 2026-08-17 | decided | `noUncheckedIndexedAccess` is off; re-enabling it is BL-010 | measured cost over ~200 tree-walking modules exceeds one session | `strict` remains a real gate | g-workspace-hygiene | n/a |
| D-025 | 2026-08-17 | decided | No `import` of a `.json` file in `packages/**` or `.claude/hooks/**`; all JSON is read through `readJsonGuarded` | D-006 requires guarded reads | The registry is hot-swappable | g-workspace-hygiene | n/a |
| D-026 | 2026-08-17 | decided | `green` splits into `green:quick`, `green:fast` (pre-commit) and `green` (+mutations, CI and `prove`); the mutation suite finishes in <= 180 s | a gate slow enough to bypass with `--no-verify` is not a gate | Pre-commit stays affordable | structural:package.json | n/a |
| D-028 | 2026-08-17 | decided | Fifteen banned behaviours are one line each in `## No theatre`; evidence prose lives in `docs/rebuild-brief/evidence.md` | prose whose only function is to explain a fixed defect is T10 | The enforcer is the enforcement | g-decisions | n/a |
| D-029 | 2026-08-17 | decided | `packages/sfs/registry/decisions.json` is generated from this file and byte-compared; there is no second registry and no `OPEN-QUESTIONS.md` | two registries with two id namespaces drift | One decision namespace | g-decisions | n/a |
| D-037 | 2026-08-17 | decided | Component versions, prop names and enum domains come from `packages/registry` only; no version integer or prop name appears in any instruction file | §1.2#11 documented 8/`accent`/`isDefaultExpanded` against reality 9/`accentStyle`/`collapsedByDefault` | Prose cannot drift from the framework | g-prose-budget | n/a |
| D-038 | 2026-08-17 | decided | The two holed gates and the DOM probe move to repo-root `quarantine/`, outside `packages/**` and outside the typecheck include, declared in `quarantine.json` | each defines a second `verdictOf` and walked/checked pair, which D-005 forbids in the live toolchain; an exemption list would be the tampering pattern | Their 15 tests still run; only the script path changed | g-coverage-single-impl | n/a |
| D-039 | 2026-08-17 | decided | The work branch is `hashim/sfs-rebuild-scope-a` cut from `8a2d2f4`; `upstream` is never pushed to | override O3; the repo was 13 commits ahead of `origin` on a local branch | P2 asserts the commit, not the branch | g-commit-format | n/a |
| D-040 | 2026-08-17 | decided | §5.1's conflicts 1, 2, 4, 5, 6, 7, 9 and 11 resolve as stated; the losing spelling is a typo wherever it appears in the brief | two authors disagreed on thirteen paths; choosing at the point of use created the three-way reflist split | `bin/sfs.mjs`, `npm run sfs`, no alias | g-decisions, g-workspace-hygiene | n/a |
| D-041 | 2026-08-17 | decided | All of §3.1.2 is defined once in `packages/registry/src/coverage.mjs`; `verify` and `sfs` re-export it with one `export *` line; `walkComponents` lives only in `packages/verify/src/walk.mjs` and is exempt from the re-export rule | §1.4's five walkers; the dependency arrow forbids the compiler importing its verifier | A compiler its verifier calls cannot audit it | g-coverage-single-impl, g-workspace-hygiene | n/a |
| D-042 | 2026-08-17 | decided | WP-0 writes the whole coverage API with >= 21 named tests; WP-3a adds `walk.mjs` and unit-typed families, not a second copy | a partial API written twice is the drift generator this rebuild removes | 23 tests ship against a floor of 21 | structural:packages/registry/test/coverage.test.mjs | n/a |
| D-043 | 2026-08-17 | decided | The brief is committed under `docs/rebuild-brief/`, outside `g-prose-budget` and `g-commands-executable`, inside `g-brief-budget` | a session that loses the brief to a reset has no source of truth; `docs/**` is not prompt payload | Skill gates never scan it | g-brief-budget | n/a |
| D-045 | 2026-08-17 | decided | `Enforced by` has five legal forms: a gate id, `structural:` path, `hook:` path, `check:<tier>:<id>`, and `pending:<WP-id>` while its WP is incomplete | a row naming a gate that cannot yet exist forced WP-0 to ship gates that check nothing | `pending:` forces the rewrite | g-decisions | n/a |
| D-046 | 2026-08-17 | decided | The brief is split by reader: `CONTROL.md` <= 25,600 B is the only turn-zero read; every >8-row table becomes `data/*.json`; every literal file becomes `artifacts/<file>` | 322,816 B of prose against a ~100-150 constraint ceiling is RC2; a 450 KB brief reproduces it one level up | BL-011 enforces the bundle target | g-brief-budget | n/a |
| D-047 | 2026-08-17 | decided | A mutation copies only that gate's declared `inputPaths[]` to a temp dir and junction-links the root `node_modules`; the suite asserts a 180 s ceiling | 110+ full-repo copies on NTFS with Defender is tens of GB per commit | `junction` is required on Windows | structural:packages/verify/test/mutation/mutation-meta.test.mjs | n/a |
| D-048 | 2026-08-17 | decided | `npm run green` writes `packages/verify/evidence/<WP>.json`; `g-commit-format` regenerates it and fails on staleness or on any commit-body number that disagrees | a commit body a gate blesses is a self-report, the over-praise failure the plan cites twice | No criterion takes a typed number | g-commit-format | n/a |
| D-051 | 2026-08-17 | decided | Scope is `packages/verify/config/session-scope.json`, eight WP ids; narrowing needs a `Scope change:` row; everything else is a BACKLOG row with its acceptance command verbatim | the alternative is discovering at 100% of budget that the compiler was unreachable | Scope cannot expand | structural:packages/verify/config/session-scope.json | n/a |
| D-056 | 2026-08-17 | decided | A pointer is `uninspectable` only when its type is in the artifact, its registry entry is incomplete, and the type is non-priority; a priority type with an incomplete entry is a fail | otherwise every screen containing a `datatable` is permanently partial and the only release valve is widening the gate | The predicate lives in `coverage.mjs` | g-coverage-single-impl | n/a |
| D-057 | 2026-08-17 | decided | After any context reset the session runs the five recovery commands and reads `CONTROL.md` before acting; `green:fast` outranks any recorded claim of completion | memory after compaction is the least reliable input available | A WP is resumed, never restarted | structural:docs/rebuild-brief/CONTROL.md | n/a |
| D-060 | 2026-08-17 | decided | `g-brief-budget`'s enforced subject in Scope A is `CONTROL.md` (<= 25,600 B, 0 fences > 40 lines); the 61,440 B whole-bundle cap and the table and fence extractions are BL-011 | the bundle is 594,452 B, 9.7x the cap; enforcing it now either fails every commit or deletes sections WP-1..WP-10 must read | The gate fails if BL-011 leaves BACKLOG.md | g-brief-budget | n/a |
| D-061 | 2026-08-17 | decided | Test discovery is a Node glob pattern over `packages/*/test/**/*.test.mjs` plus `quarantine/*.test.mjs` | on Node >= 22 a positional argument to `--test` is a file pattern, not a directory to scan, and cmd.exe does not expand globs | `npm test` actually runs | structural:package.json | n/a |
| D-062 | 2026-08-17 | decided | Legal skill frontmatter keys are `name`, `description`, `allowed-tools` and `argument-hint`, read from config | `allowed-tools` and `argument-hint` are real Claude Code keys; stripping `allowed-tools` widens a skill's tool access | Smuggled config stays blocked | g-prose-budget | n/a |
| D-063 | 2026-08-17 | decided | Pre-existing prose debt is carried as measured waivers, each naming its path, allowance, the WP or BL id that removes it, and this row; `--baseline` refuses to raise one | a rule firing 34 times on day one gets waived wholesale unless the debt is quantified and dated | A waiver outliving its WP fails | g-prose-budget | n/a |
| D-064 | 2026-08-17 | decided | Every gate reads through `readText`, which strips a BOM and normalises CRLF to LF, and caps are measured against normalised bytes | `core.autocrlf=true` gives the tree CRLF while git stores LF, so a line-anchored regex matched nothing locally and everything in CI | One rule, one meaning, both platforms | structural:packages/verify/src/lib/fsx.mjs | n/a |
| D-065 | 2026-08-17 | decided | A `pending-probe` Confirmation carries its probe name as `pending-probe:<name>`, and `packages/registry/probes/<name>.mjs` must exist | a bare `pending-probe` gives the gate no script to resolve, making the rule unenforceable | Every empirical row has a probe | g-decisions | n/a |
| D-066 | 2026-08-17 | decided | Source-scanning patterns live in `packages/verify/config/source-patterns.json`, never as literals in gate source | a detector carrying its patterns inline matches its own file: both gates reported themselves as violations | No self-exemption; an exemption list is the tampering shape | g-coverage-single-impl, g-workspace-hygiene | n/a |
| D-067 | 2026-08-17 | decided | Every mutation first asserts the gate PASSES on an unmutated copy of its own declared `inputPaths`, before applying the mutation | otherwise a gate already failing on its staged inputs "catches" every mutation for the wrong reason; this found 9 such cases | A gate declares every path it reads | structural:packages/verify/test/mutation/mutation-meta.test.mjs | n/a |
| D-069 | 2026-08-17 | decided | `.githooks/pre-commit` runs the full `npm run green`, not `green:fast`; `green:fast` remains the fast local signal | the hook produces `evidence/<WP>.json`, and evidence without mutation numbers is a self-report about the one property making a gate falsifiable | Pre-commit costs ~45 s once per WP | hook:.githooks/pre-commit | n/a |
| D-070 | 2026-08-17 | decided | Scope change: WP-1 splits into WP-1.a (the Q1/Q2 GO/NO-GO core) and WP-1.b (Q5 determinism, golden-defect classes, `cost-delta`, `g-artifact-naming`); both stay in scope | WP-1's 25 artifacts exceed one context window and CONTROL §6 forbids half-committing, so the split is data declared before the work | Nine ids in scope; no work leaves the session | structural:packages/verify/config/session-scope.json | n/a |
| D-044 | 2026-08-17 | decided | Exactly four artifact names: `<screen>.form.json`, `<screen>.compile.json`, `<screen>.form.meta.json`, `<screen>.sfs.meta.json`; blessed fixtures are `<screen>.expected.form.json`; `.compiled.json` is banned | three names for two artifacts were embedded in command lines the session would paste | No path may match `\.compiled(\.meta)?\.json$` | g-artifact-naming | n/a |
| D-071 | 2026-08-17 | decided | The Q2 oracle's second arm, `packages/sfs/tools/normalise-legacy.mjs`, is authored by a fresh-context subagent that never sees `src/compile/**`; its write glob is exactly that one file | the gate proves import independence, not authorial: one author writing both arms lets a shared misconception about N1-N10 agree with itself | Overrides §7 for one declared slice, accepted by a program | g-oracle-independence | n/a |
| D-073 | 2026-08-17 | decided | The `pending:` budget counts DISTINCT owner ids, not rows; `pending-budget.json` `max` is 20 and ratchets down | O6 fixes max at 20, a figure §1.4 derived from a 37-row seed set; the registry now holds 72 rows and 35 defer, so a row count can never reach 20 while the deferred work genuinely exists | 8 distinct owners against a cap of 20 | g-decisions | n/a |
| D-074 | 2026-08-17 | decided | The name-independent oracle comparison is `packages/sfs/src/lib/normalForm.mjs`, and Q2 compares `normalForm(compile(decompile(m)))` against `normalForm(normaliseLegacy(m))` over the same envelope `m`; `tools/pnf.mjs` and the hand-written-fixture form of Q2 are the losing spellings | §2.4.5 voids the unsound form: ids derive from author-chosen names, so a hand-written fixture and a decompiled legacy form cannot agree. Both arms must start from one source | Q2 is falsifiable, not unachievable; `normalForm` also drops `componentName` per §2.5 step 7 | structural:packages/sfs/src/lib/normalForm.mjs | n/a |
| D-075 | 2026-08-17 | decided | The registry is the UNION of `/DECISIONS.md` and `docs/decisions-archive.md`; `gen-decisions --archive` moves only closed rows (`decided`, no `pending:` enforcer) until the live file is under `decisions-archive.json`'s target | the live file is prompt payload and capped, and the cap was being paid for by trimming rows' own prose; the backlogged superseded-by selector frees nothing, because no row is superseded | Archiving moves bytes out of the prompt, never a rule out of a gate: `g-decisions` walks the union, and the archive stays in `g-commands-executable`'s scan set so a floor cannot drain by archiving | g-decisions | n/a |
| D-076 | 2026-08-17 | decided | The decompiler is lossless by default: step 7 drops a prop only where the compiler provably regenerates it byte-identically; every other non-default prop is preserved, in a typed `raw` block when SFS cannot express it | Q2 requires `compile(decompile(m))` to differ from `m` by exactly N1..N12; a lossy decompiler adds a thirteenth difference no oracle rule accounts for | Q2 is achievable; every drop is justified by a regeneration proof, not by convenience | g-oracle-independence | n/a |
| D-077 | 2026-08-17 | decided | N12: the compiler stamps `editMode:"editable"` on the `actions` group AND on every item; the oracle contract states this as a normalisation rule | §2.1.7 requires it (an inherited button renders disabled, debug.md row 31) while the golden carries `inherited` on the item, so without N12 the two Q2 arms diverge on that one key | The oracle's contract is N1..N12, not N1..N11; both arms rewrite the item's editMode | g-oracle-independence | n/a |
| D-078 | 2026-08-18 | decided | `normalForm` is a TOTAL order: it re-serialises with recursively sorted keys, positionalises slot wrapper ids (`0#content`) and `actionOwner` values at any depth, alongside node ids and parentIds | key insertion order and id spelling are emitter properties Q1 already pins byte-for-byte; two independent programs can only be asked to agree on content, and production slot ids are nanoids while the compiler stamps v5 | Q2's byte equality is exactly content equality with no serialisation escape hatch | structural:packages/sfs/src/lib/normalForm.mjs | n/a |
| D-004 | 2026-08-17 | decided | The registry is generated from a pinned `shesha-framework` `releases/0.45` SHA; the framework is never modified | §4 L0 | Registry data is reproducible | g-registry-completeness | n/a |
| D-079 | 2026-08-18 | decided | Measured production shapes are emitted, never normalised away: `_type:"action-config"` on every action config, `caption:""`/`isVisible:true` on the crud-operations column, `maxWidth:null` carried via column `max`, real inline editors via column `editor:{type,props}`; `actionArguments` is omitted when empty; a child of a row with no `responsive` is width `auto`, never the 100% dimension default | the golden and the corpus form carry these bytes; dropping an inline table's editors is data loss (D-076), and two 100% children in a row block is the N9 geometry | the SFS column grammar gains `editor` and `max`; both Q2 arms agree on the measured shapes | g-oracle-independence | n/a |
| D-080 | 2026-08-18 | decided | Test change: A1's non-vacuity guard is per-fixture on `components` and `items` and SET-level on `slots`; the identity `ids = components + slots + items` is unchanged and still total | `slots` is only produced by a slotted type, so a per-fixture `slots > 0` silently requires every clean fixture to carry a page shell — a design constraint no rule states, and fixture-fitting to satisfy it would be the defect this rebuild removes | a fixture may faithfully mirror a shell-less production form; at least one fixture must still stamp a slot, asserted over the set | structural:packages/sfs/test/golden-defects.test.mjs | n/a |
| D-081 | 2026-08-18 | decided | The golden-defect ratchet asserts, over both legacy subjects, that every class `measure-form.mjs` finds PRESENT is ABSENT from `compile(decompile(subject))`; the count is discovered and printed, never a literal; at least one subject must carry a defect | "the compiler fixes the defects" is only evidence when measured on the same artifact in both directions by one predicate vocabulary; a hand-copied count is theatre | the census files are generated (`measure-form.mjs --defects --json`) and byte-checked by the test | structural:packages/sfs/test/defect-ratchet.test.mjs | n/a |
| D-082 | 2026-08-18 | decided | `cost-baseline.json` lives at `packages/sfs/config/`, not `packages/verify/config/`: `packages/sfs/tools/cost-delta.mjs` reads it and D-041 forbids an sfs module depending on verify; the WP-10 `g-cost-delta` gate reads it across the allowed L3->L1 direction | §5.2's `packages/verify/config/cost-baseline.json` path inverts the layer the moment a compiler-side tool reads it, which `g-workspace-hygiene` dep-direction catches | one config, read down-layer only | g-workspace-hygiene | n/a |
| D-083 | 2026-08-18 | decided | The SFS-node -> component-type reverse map is GENERATED in `loadRegistry` from each record's `sfsNode`/`sfsNodeAlso`, never hand-written | a hand-kept reverse map is the standard way two directions of the same table drift apart | one direction is authored, the other derived | structural:packages/sfs/src/lib/registry.mjs | n/a |
| D-084 | 2026-08-18 | decided | `columns` and `sizableColumns` are `authorable:false`: a table's columns are authored as `columns[]` on the table node and expanded to `items[]`, never as standalone components | two ways to author the same structure is the ambiguity the SFS grammar removes | `recordForNode` throws SFS-1004 on either | g-registry-completeness | n/a |
| D-010 | 2026-08-17 | decided | `bake-overlays.mjs` is deleted; `$role:` tokens resolve at compile time, per run, per brand | §1.2 | No separate styling pass to forget | structural:packages/sfs/src/lib/tokens.mjs | n/a |
| D-013 | 2026-08-17 | decided | Raw seed JSON under `assets/examples/` is deleted only after decompilation into `packages/sfs/corpus/`; `raw:` escapes are counted per compile | §5 | Seeds cannot vanish unconverted | g-corpus-immutable | n/a |
| D-019 | 2026-08-17 | decided | Literal hex colours are banned in recipes, SFS and compiler input; output carries resolved colours each with a `resolvedFrom` entry | §1.2#13 48 hexes vs 10 role refs | Colour provenance is traceable | structural:packages/sfs/src/lib/tokens.mjs | n/a |
