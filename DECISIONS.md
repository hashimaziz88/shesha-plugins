## Decisions

| ID | Date | Status | Decision | Why | Consequence | Enforced by | Confirmation |
|---|---|---|---|---|---|---|---|
| D-001 | 2026-08-17 | decided | Adopt the compiler-first re-architecture on the PR #39 base; reject prose iteration and clean-sheet | §3 | Assets are moved, not re-earned | pending:WP-7a | n/a |
| D-003 | 2026-08-17 | decided | The compiler is the only writer of form markup; direct writes are blocked, not discouraged | §4 L4 | No hand-edited form JSON | pending:BL-008 | n/a |
| D-004 | 2026-08-17 | decided | The registry is generated from a pinned `shesha-framework` `releases/0.45` SHA; the framework is never modified | §4 L0 | Registry data is reproducible | pending:WP-2 | n/a |
| D-008 | 2026-08-17 | decided | A missing backend degrades to `uninspectable` + exit 3, never pass; exits are 0/1/2/3; the verdict union is exactly `{pass,fail,partial,notRun}`, no `warn` | §1.4 | backend-absent is never a pass | pending:WP-3a | n/a |
| D-010 | 2026-08-17 | decided | `bake-overlays.mjs` is deleted; `$role:` tokens resolve at compile time, per run, per brand | §1.2 | No separate styling pass to forget | pending:WP-5 | n/a |
| D-011 | 2026-08-17 | decided | `validate-blocks.js` is deleted; its checks become T2 hard failures with exact registry matching | §1.4 | No fuzzy matcher with a downgrade | pending:WP-7a | n/a |
| D-013 | 2026-08-17 | decided | Raw seed JSON under `assets/examples/` is deleted only after decompilation into `packages/sfs/corpus/`; `raw:` escapes are counted per compile | §5 | Seeds cannot vanish unconverted | pending:WP-5 | n/a |
| D-014 | 2026-08-17 | decided | Placement assertions are executable predicates over the compiled tree, not English sentences judged by a model | §4 L3 | The DOM probe covers residue only | pending:BL-003 | n/a |
| D-015 | 2026-08-17 | decided | T5 visual is advisory: `verdict.result` is computed from T1-T4 only and is byte-identical whichever score T5 reports | §4 L3 | No T5 finding downgrades a result | pending:BL-005 | n/a |
| D-016 | 2026-08-17 | decided | The judge never sees the builder's self-report or reasoning | §4 L3 | The Evaluator cannot read run logs | pending:BL-008 | n/a |
| D-017 | 2026-08-17 | decided | Fan out across screens only; never run parallel agents within one form | §4 L4 | Fan-out is a declared glob partition | pending:BL-008 | n/a |
| D-018 | 2026-08-17 | decided | Styling is a compile-time concern; there is no separate styling pass and no styling sub-skill invocation | §1.2#5 | One pass produces styled output | pending:WP-7a | n/a |
| D-019 | 2026-08-17 | decided | Literal hex colours are banned in recipes, SFS and compiler input; output carries resolved colours each with a `resolvedFrom` entry | §1.2#13 48 hexes vs 10 role refs | Colour provenance is traceable | pending:WP-5 | n/a |
| D-021 | 2026-08-17 | decided | Compilation is deterministic: no clock, no randomness; ids are `uuidv5(NS, "<module>/<form>\|<path>")` | §4 L2 | Identical input, identical bytes | pending:WP-5 | n/a |
| D-022 | 2026-08-17 | decided | Ambiguity in headless mode is resolved by an archetype default recorded in `plan.json` | §1.2#12 | Never ask, never silently pick | pending:BL-008 | n/a |
| D-027 | 2026-08-17 | decided | `test.todo(` is legal only under `packages/sfs/test/fixtures/gaps/` or in `*.gap.test.mjs`, with a `GAP-0NN` id present in BACKLOG.md | §5.5 B4 needs a visible failing fixture per compiler gap | Compiler gaps stay visible | pending:WP-10 | n/a |
| D-030 | 2026-08-17 | pending-probe | `referenceListId` is always `{module, name}`; a module prefix inside `name` is illegal everywhere | §1.2#1 three-way split; observed `ConfigurationLoadingError` blocks the whole form | Flat and prefixed shapes hard-fail | pending:WP-3a | pending-probe:reflist-name-format |
| D-031 | 2026-08-17 | pending-probe | `refListStatus` carries `referenceListId: {module,name}` and nothing else; flat `module` / `referenceListName` keys hard-fail | §1.2#2 settled in one file while the routed-to file shipped the flat keys with a warning not to correct it | Two authors cannot ship two shapes | pending:WP-3a | pending-probe:reflist-status-shape |
| D-032 | 2026-08-17 | pending-probe | `editMode` is emitted only at form level, derived from `kind`: list/details -> `readOnly`, create/edit -> `editable`; no component carries it | §1.2#7 three answers to one question | SFS has no component-level field | pending:WP-3a | pending-probe:editmode-create-edit |
| D-033 | 2026-08-17 | pending-probe | The only legal `formArguments` hydration hook is `onAfterDataLoad`; `onDataLoaded` is illegal in compiler output | §1.2#8 three files, three hooks; the observed modal failure outranks prose | `forbidden` fails only when non-null | pending:WP-3a | pending-probe:form-arguments-hook |
| D-034 | 2026-08-17 | pending-probe | `actionOwner` is lowercase dotted or a compiler-resolved component id; `actionName` is the spaced form; SFS never names an owner | §1.2#9 the documented strings are the ones blamed for silent no-op buttons | The compiler derives it from the verb | pending:WP-3a | pending-probe:action-owner-casing |
| D-035 | 2026-08-17 | pending-probe | The `columns` component is banned unconditionally; multi-column layout is a flex `container` row sized by `desktop.dimensions.width` | §1.2#4 banned in five places, mandated in a sixth 52 lines earlier | No recipe, form or output contains one | pending:WP-3a | pending-probe:columns-alternative |
| D-036 | 2026-08-17 | pending-probe | Entity metadata is read from `Get` and unwrapped as `result.properties[]`; paths are PascalCase in metadata and camelCased when emitted | §1.2#10 wrong casing renders every cell blank | One unwrapper, one casing rule | pending:BL-003 | pending-probe:metadata-endpoint |
| D-049 | 2026-08-17 | decided | `verify-artifact.mjs` and `check-references.mjs` move unchanged, are referenced by nothing, and are registered in `quarantine.json` with `liftedBy: BL-003` | Phase 0 wants the bleeding stopped now, but the correct fixes need T2/T3 | A holed gate emits no green signal | pending:BL-003 | n/a |
| D-050 | 2026-08-17 | decided | `cost-delta` gates exactly two recomputable byte counts, `emittedBytes` (floor 10x) and `preloadBytes` (floor 5x); steps, tokens and tool calls move to BL-001 | a ratio derived from prose the session is deleting is T15 | `prove` prints `token cost: unmeasured in this session` | pending:WP-10 | n/a |
| D-052 | 2026-08-17 | decided | Repairs on one gate in one WP are capped at 3 rounds; at round 3 the choice is block the WP or fix the gate with its mutation, never silence it | UI2Code^N saturation at N=3-5; more iterations sometimes amplify reward hacking | Round four adapts the test to the code | pending:WP-10 | n/a |
| D-053 | 2026-08-17 | decided | SFS gains the node kinds `childTable`, `kib`, `tags`, `attachments`, `picker`, `select`, and `field` as the catch-all for every registry type with `isInput: true` | §2.1.4's 17 kinds cover none of what the corpus is made of | Without these every form is `raw:` | pending:WP-5 | n/a |
| D-054 | 2026-08-17 | decided | Round-trip is gated at >= 0.90 over the six declared forms in `roundtrip-scope.json`; the other six are decompiled, triaged and reported `uninspectable` with a B8 row | a 0.90 gate over 12 forms whose kinds do not exist yet is unachievable, and an unachievable gate is lowered under pressure | BL-002 gates the full corpus | pending:WP-5 | n/a |
| D-055 | 2026-08-17 | decided | `forbidden` in `form-settings.json` means forbidden with a non-null value: fail iff the key is present and not `null`; fail on any key outside `allowed` union `forbidden` | §2.1.3 emits `onBeforeDataLoad: null` for every kind, which a literal reading would fail on the one artifact everything is measured against | A null forbidden key is legal | pending:WP-3a | n/a |
| D-058 | 2026-08-17 | decided | Fan-out only for disjoint-write, no-shared-invariant, program-verifiable, enumerable slices, max 4 agents; subagents never commit and never mutate `node_modules` or the index | §4/L4 excludes shared-context coding from multi-agent gains; two parallel authors already shipped two incompatible `refListStatus` shapes | A self-report is not evidence | pending:WP-10 | n/a |
| D-059 | 2026-08-17 | decided | Gate removal: `g-fanout-discipline`'s two-directory rule | it failed every commit shipping an implementation together with the gate policing it, i.e. WP-1, WP-5, WP-7a and WP-3a | The declared-glob partition proof already covers the anti-pattern | pending:WP-10 | n/a |
| D-068 | 2026-08-17 | decided | A gate asserts only over its own declared subject; a rule about a neighbouring concern belongs to the gate that owns that concern | `g-workspace-hygiene` failing six pre-existing skill helpers would delete working behaviour to satisfy a rule `g-skill-purity` owns | Skill purity is adjudicated once, at WP-7a | pending:WP-7a | n/a |
| D-072 | 2026-08-17 | decided | The plugin version is valid semver including prerelease identifiers, so `1.8.5-alpha.1` and never `1.8.5alpha1` | `g-plugin-version` must prove the version strictly increases and a semver parser rejects an unseparated prerelease suffix | Marketplace and gate order it the same way | pending:WP-10 | n/a |
| D-074 | 2026-08-17 | decided | The name-independent oracle comparison is `packages/sfs/src/lib/normalForm.mjs`, and Q2 compares `normalForm(compile(decompile(m)))` against `normalForm(normaliseLegacy(m))` over the same envelope `m`; `tools/pnf.mjs` and the hand-written-fixture form of Q2 are the losing spellings | §2.4.5 voids the unsound form: ids derive from author-chosen names, so a hand-written fixture and a decompiled legacy form cannot agree. Both arms must start from one source | Q2 is falsifiable, not unachievable; `normalForm` also drops `componentName` per §2.5 step 7 | structural:packages/sfs/src/lib/normalForm.mjs | n/a |
| D-075 | 2026-08-17 | decided | The registry is the UNION of `/DECISIONS.md` and `docs/decisions-archive.md`; `gen-decisions --archive` moves only closed rows (`decided`, no `pending:` enforcer) until the live file is under `decisions-archive.json`'s target | the live file is prompt payload and capped, and the cap was being paid for by trimming rows' own prose; the backlogged superseded-by selector frees nothing, because no row is superseded | Archiving moves bytes out of the prompt, never a rule out of a gate: `g-decisions` walks the union, and the archive stays in `g-commands-executable`'s scan set so a floor cannot drain by archiving | g-decisions | n/a |
| D-076 | 2026-08-17 | decided | The decompiler is lossless by default: step 7 drops a prop only where the compiler provably regenerates it byte-identically; every other non-default prop is preserved, in a typed `raw` block when SFS cannot express it | Q2 requires `compile(decompile(m))` to differ from `m` by exactly N1..N12; a lossy decompiler adds a thirteenth difference no oracle rule accounts for | Q2 is achievable; every drop is justified by a regeneration proof, not by convenience | g-oracle-independence | n/a |
| D-077 | 2026-08-17 | decided | N12: the compiler stamps `editMode:"editable"` on the `actions` group AND on every item; the oracle contract states this as a normalisation rule | §2.1.7 requires it (an inherited button renders disabled, debug.md row 31) while the golden carries `inherited` on the item, so without N12 the two Q2 arms diverge on that one key | The oracle's contract is N1..N12, not N1..N11; both arms rewrite the item's editMode | g-oracle-independence | n/a |
| D-078 | 2026-08-18 | decided | `normalForm` is a TOTAL order: it re-serialises with recursively sorted keys, positionalises slot wrapper ids (`0#content`) and `actionOwner` values at any depth, alongside node ids and parentIds | key insertion order and id spelling are emitter properties Q1 already pins byte-for-byte; two independent programs can only be asked to agree on content, and production slot ids are nanoids while the compiler stamps v5 | Q2's byte equality is exactly content equality with no serialisation escape hatch | structural:packages/sfs/src/lib/normalForm.mjs | n/a |
| D-079 | 2026-08-18 | decided | Measured production shapes are emitted, never normalised away: `_type:"action-config"` on every action config, `caption:""`/`isVisible:true` on the crud-operations column, `maxWidth:null` carried via column `max`, real inline editors via column `editor:{type,props}`; `actionArguments` is omitted when empty; a child of a row with no `responsive` is width `auto`, never the 100% dimension default | the golden and the corpus form carry these bytes; dropping an inline table's editors is data loss (D-076), and two 100% children in a row block is the N9 geometry | the SFS column grammar gains `editor` and `max`; both Q2 arms agree on the measured shapes | g-oracle-independence | n/a |
| D-080 | 2026-08-18 | decided | Test change: A1's non-vacuity guard is per-fixture on `components` and `items` and SET-level on `slots`; the identity `ids = components + slots + items` is unchanged and still total | `slots` is only produced by a slotted type, so a per-fixture `slots > 0` silently requires every clean fixture to carry a page shell — a design constraint no rule states, and fixture-fitting to satisfy it would be the defect this rebuild removes | a fixture may faithfully mirror a shell-less production form; at least one fixture must still stamp a slot, asserted over the set | structural:packages/sfs/test/golden-defects.test.mjs | n/a |
| D-081 | 2026-08-18 | decided | The golden-defect ratchet asserts, over both legacy subjects, that every class `measure-form.mjs` finds PRESENT is ABSENT from `compile(decompile(subject))`; the count is discovered and printed, never a literal; at least one subject must carry a defect | "the compiler fixes the defects" is only evidence when measured on the same artifact in both directions by one predicate vocabulary; a hand-copied count is theatre | the census files are generated (`measure-form.mjs --defects --json`) and byte-checked by the test | structural:packages/sfs/test/defect-ratchet.test.mjs | n/a |
| D-082 | 2026-08-18 | decided | `cost-baseline.json` lives at `packages/sfs/config/`, not `packages/verify/config/`: `packages/sfs/tools/cost-delta.mjs` reads it and D-041 forbids an sfs module depending on verify; the WP-10 `g-cost-delta` gate reads it across the allowed L3->L1 direction | §5.2's `packages/verify/config/cost-baseline.json` path inverts the layer the moment a compiler-side tool reads it, which `g-workspace-hygiene` dep-direction catches | one config, read down-layer only | g-workspace-hygiene | n/a |

## No theatre

```
T1  Shipping a script that cannot execute -> g-commands-executable
T2  A gate that warns where the docs say it fails -> g-gate-contract (mutations flip verdicts, not warning counts)
T3  A program that writes despite declaring it refuses to -> D-010, pending:WP-5
T4  A pass with zero coverage -> structural:packages/registry/src/coverage.mjs (D-006)
T5  Claiming coverage of territory never visited -> g-coverage-single-impl + walk.mjs over items/columns/tabs
T6  Lazily-created report families that can vanish -> families() declared up front (D-005) + expectEmpty
T7  A gate wired into nothing -> the gate ratchet in run-gates.mjs (inputPaths must exist and be non-empty)
T8  A documented MUST-strength gate that ships no code -> g-commands-executable + D-002
T9  Resolving a contradiction by annotating it -> D-009 + g-prose-budget
T10 Changelog archaeology in an instruction file -> g-prose-budget archaeology patterns
T11 A prose rule with no enforcing program -> g-decisions (every row names a live or pending: enforcer)
T12 An unreproducible generated asset -> pending:WP-2 (pinned ref; no drive-letter path)
T13 Feeding the builder's self-report to the judge -> D-016, pending:BL-008
T14 Fuzzy matchers with escape hatches that downgrade failures -> D-011 + exact registry matching
T15 Presenting an unmeasurable quantity as a measurement -> D-014, D-050; unseeable is uninspectable
```
