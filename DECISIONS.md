## Decisions

| ID | Date | Status | Decision | Why | Consequence | Enforced by | Confirmation |
|---|---|---|---|---|---|---|---|
| D-003 | 2026-08-17 | decided | The compiler is the only writer of form markup; direct writes are blocked, not discouraged | §4 L4 | No hand-edited form JSON | pending:BL-008 | n/a |
| D-011 | 2026-08-17 | decided | `validate-blocks.js` is deleted; its checks become T2 hard failures with exact registry matching | §1.4 | No fuzzy matcher with a downgrade | pending:WP-3a | n/a |
| D-015 | 2026-08-17 | decided | T5 visual is advisory: `verdict.result` is computed from T1-T4 only and is byte-identical whichever score T5 reports | §4 L3 | No T5 finding downgrades a result | pending:BL-005 | n/a |
| D-016 | 2026-08-17 | decided | The judge never sees the builder's self-report or reasoning | §4 L3 | The Evaluator cannot read run logs | pending:BL-008 | n/a |
| D-017 | 2026-08-17 | decided | Fan out across screens only; never run parallel agents within one form | §4 L4 | Fan-out is a declared glob partition | pending:BL-008 | n/a |
| D-022 | 2026-08-17 | decided | Ambiguity in headless mode is resolved by an archetype default recorded in `plan.json` | §1.2#12 | Never ask, never silently pick | pending:BL-008 | n/a |
| D-030 | 2026-08-17 | pending-probe | `referenceListId` is always `{module, name}`; a module prefix inside `name` is illegal everywhere | §1.2#1 three-way split; observed `ConfigurationLoadingError` blocks the whole form | Flat and prefixed shapes hard-fail | pending:WP-3a | pending-probe:reflist-name-format |
| D-031 | 2026-08-17 | pending-probe | `refListStatus` carries `referenceListId: {module,name}` and nothing else; flat `module` / `referenceListName` keys hard-fail | §1.2#2 settled in one file while the routed-to file shipped the flat keys with a warning not to correct it | Two authors cannot ship two shapes | pending:WP-3a | pending-probe:reflist-status-shape |
| D-032 | 2026-08-17 | pending-probe | `editMode` is emitted only at form level, derived from `kind`: list/details -> `readOnly`, create/edit -> `editable`; no component carries it | §1.2#7 three answers to one question | SFS has no component-level field | pending:WP-3a | pending-probe:editmode-create-edit |
| D-033 | 2026-08-17 | pending-probe | The only legal `formArguments` hydration hook is `onAfterDataLoad`; `onDataLoaded` is illegal in compiler output | §1.2#8 three files, three hooks; the observed modal failure outranks prose | `forbidden` fails only when non-null | pending:WP-3a | pending-probe:form-arguments-hook |
| D-034 | 2026-08-17 | pending-probe | `actionOwner` is lowercase dotted or a compiler-resolved component id; `actionName` is the spaced form; SFS never names an owner | §1.2#9 the documented strings are the ones blamed for silent no-op buttons | The compiler derives it from the verb | pending:WP-3a | pending-probe:action-owner-casing |
| D-035 | 2026-08-17 | pending-probe | The `columns` component is banned unconditionally; multi-column layout is a flex `container` row sized by `desktop.dimensions.width` | §1.2#4 banned in five places, mandated in a sixth 52 lines earlier | No recipe, form or output contains one | pending:WP-3a | pending-probe:columns-alternative |
| D-036 | 2026-08-17 | pending-probe | Entity metadata is read from `Get` and unwrapped as `result.properties[]`; paths are PascalCase in metadata and camelCased when emitted | §1.2#10 wrong casing renders every cell blank | One unwrapper, one casing rule | check:t3-semantic:T3.02 | pending-probe:metadata-endpoint |
| D-049 | 2026-08-17 | superseded-by-D-109 | `verify-artifact.mjs` and `check-references.mjs` move unchanged, are referenced by nothing, and are registered in `quarantine.json` with `liftedBy: BL-003` | Phase 0 wants the bleeding stopped now, but the correct fixes need T2/T3 | A holed gate emits no green signal | g-check-references | n/a |
| D-055 | 2026-08-17 | decided | `forbidden` in `form-settings.json` means forbidden with a non-null value: fail iff the key is present and not `null`; fail on any key outside `allowed` union `forbidden` | §2.1.3 emits `onBeforeDataLoad: null` for every kind, which a literal reading would fail on the one artifact everything is measured against | A null forbidden key is legal | pending:WP-3a | n/a |
| D-089 | 2026-08-18 | superseded-by-D-095 | The `components-kb` and `capability-matrix.json` are NOT moved into `packages/registry/data`: WP-0's two disposition move rows are void per §2.8.1 ("the KB stays as the extractor's intermediate") and §2.8.5 (the matrix is a design-skill asset; only its `versions` map is deleted, D-087) | `gen-registry.mjs` reads the KB in place under `plugins/**`; relocating a 920 KB read-only input or a design-skill asset into L0 data would invert ownership | disposition.json carries no WP-2 move row; the KB and matrix keep their plugins paths | g-disposition | n/a |
| D-102 | 2026-08-19 | decided | Scope change: WP-1c delivers BL-010 (noUncheckedIndexedAccess on tree-wide; g-workspace-hygiene enforces it true). BL-001 (3-arm SAA cost, unmeasurable in-session per D-050), BL-006, BL-014 stay BACKLOG, out of Scope B | BL-001 cannot be measured here, so bundling blocks WP-1c forever | deferred work stays visible in BACKLOG, never dropped | g-workspace-hygiene | n/a |
| D-103 | 2026-08-20 | decided | Scope change: WP-3b (full brief T3) splits into WP-3b.1 (compiler placement sidecar §3.3.1), WP-3b.2 (18-name predicate engine + assertions.schema.json), WP-3b.3 (the T3 tier), WP-3b.4 (lift g-check-references out of quarantine); session-scope-b.json swaps WP-3b for the four | placement needs an L1 sidecar first, and g-check-references cannot lift while 12 dangling refs fail its baseline; each exceeds one context (CONTROL §6, mirrors D-096/D-098) | prove-b requires all four | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |
| D-104 | 2026-08-20 | decided | The compiler emits the §3.3.1 sidecar (per node region, depth, parent, tabKey, cell{sizing,px,reservePx}, rowGroup, align, orientation) on every compile, validated against packages/sfs/schema/compiled-meta.schema.json; T3 and the placement predicates read placement from it, not CSS; cell.sizing is the compiler's calc(100%-Npx)/Npx/auto declaration | a 1fr intent reads in the DOM as a fixed px width, so placement comes from the compiler | the contract WP-3b.2/3b.3 stand on | g-compiled-meta | n/a |
| D-105 | 2026-08-20 | decided | Placement predicates are the frozen 18-name registry (packages/verify/config/predicates.json), implemented in predicates/index.mjs and enumerated in assertions.schema.json; the three must agree; a contract is declarative predicate rows the verifier evaluates over the sidecar, never English/eval | three copies of the name set drift unless a program reconciles them (D-014) | g-no-prose-assertions bans prose assertions and fails on registry drift | g-no-prose-assertions | n/a |
| D-106 | 2026-08-20 | decided | Scope change: WP-3b.3 (the T3 tier) splits into WP-3b.3 (offline-core tier, wired into verify.mjs, the .build/wp3b --tiers t1,t2,t3 exit-0 acceptance), WP-3b.3b (contract checks T3.20/21/22 + fixtures + g-fixture-manifest/g-verdict-integrity), WP-3b.3c (metadata substrate + the 6 backend checks, closing D-036); session-scope-b.json swaps WP-3b.3 for the three | offline checks are green now; backend needs a snapshot substrate; contract needs fixtures (CONTROL §6) | prove-b requires all three | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |
| D-107 | 2026-08-20 | decided | The T3 contract checks (T3.20 columns, T3.21 placement, T3.22 tabs) read a per-screen contract fixture (packages/sfs/test/fixtures/contracts/&lt;screen&gt;.contract.json: acceptance predicate rows + a declared column set) and evaluate it through the WP-3b.2 engine; verify.mjs loads it, a run-dir copy overriding the committed one | placement drift is caught by recomputing the contract over the freshly-compiled screen, not by trusting a reported result | g-fixture-manifest validates each contract (schema, known predicates, real screen, byte cap); g-verdict-integrity recomputes it and fails on drift | g-verdict-integrity | n/a |
| D-108 | 2026-08-20 | decided | The T3 backend checks (T3.01/02/06/07/09) resolve against a recorded metadata snapshot (packages/sfs/test/fixtures/metadata/&lt;screen&gt;.metadata.json, D-036 shape) that verify.mjs auto-loads per screen; absent a snapshot each disposes uninspectable, never a pass, and the bounded set lives in uninspectable-budget.json. T3.03/T3.05/T3.11 stay deferred — they need entity-metadata or registry data (datatype-components, action-owners) sourced not guessed (WP-2b) | a 1fr-style casing defect renders every cell blank (§1.2#10) yet the DOM cannot see it, so the check reads the compiler binding against recorded metadata; an unbounded "could not look" licence is how a verifier stops verifying | g-uninspectable-budget bounds the uninspectable set; the snapshot keeps BL-003 at exit 0 | g-uninspectable-budget | n/a |
| D-109 | 2026-08-20 | decided | WP-3b lifts the two BL-003 quarantined files: t3-semantic.mjs is rewritten as the T3 tier (WP-3b.3), check-references.mjs as g-check-references on the one coverage impl (WP-3b.4) — lazy-families + uncaught-SyntaxError holes closed; the overlays/versions families drop (subjects gone: blocks D-010, components-kb to packages/sfs/kb D-095); the quarantine.json BL-003 rows and superseded files are removed. Supersedes D-049 | a holed gate emits no green signal, but the fixes needed the T2/T3 layer that now exists | 68 dead shesha-form-edit refs repointed to shesha-spec or removed; only layout-probe stays quarantined | g-check-references | n/a |
| D-110 | 2026-08-20 | decided | Precedent retrieval (BL-009, WP-9) is Index A: a deterministic SHAPE index (kind, node multiset, region topology, columns, actions, tabs) over the compiled corpus markup, stored one-per-line in packages/precedent/data/shapes.jsonl (gitignored) — no node:sqlite (flag-gated on Node 22, unflagged import throws), no embedding (Index B is BL-H1). similarity = 0.5·nodeMultiset-jaccard + 0.3·topology-trigram-jaccard + 0.2·kind; the trigram proxy keeps a 5000-row scan under 50ms. RAG is never a correctness lookup: the compiler/verifier never import it, it never imports the compiler, no skill routes props/versions/enums to it | an exact registry answer beats a nearest-neighbour; retrieval over the KB reintroduces the ambiguity the registry removes (§4.7.1) | the package ships behaviour (no E_NOT_IMPLEMENTED); an empty index is a hard error and an embedding request degrades to shape | g-rag-isolation | n/a |
| D-112 | 2026-08-21 | decided | Test change: the WP-9 precedent perf test's wall-clock budget is raised 50 -&gt; 120ms because `npm test` runs test files in parallel and this single reading absorbs co-runner scheduler/GC noise (measured 50-70ms under suite load, ~40ms in isolation); D-110's design intent and the algorithm's isolated cost are unchanged, and 120ms still fails hard on the O(n^2) regression the budget guards | §4.7.2 | No wall-clock assertion that flakes under the parallel runner | structural:packages/precedent/test/shape.test.mjs | n/a |
| D-113 | 2026-08-21 | decided | WP-2b lifts the registry from 12 to 93 `full`: the reproducible extractor parse-framework-props.mjs source-parses each component's settings form (editor kind -&gt; valueType, omit-never-guess, base-only counts only when no real editor is dropped) to type ~80 components beyond the 13 priority anchors; `full &gt;= 93` and `deferredAuthorable &lt;= 7` become §2.8.4 demands; completenessOf counts the compiler's `framework-verified` props (container.direction, dataContext.uniqueStateId, datatable.crud) as full-derived alongside source-parsed, and a null overlay prop never clobbers a source-parsed one; `paragraph` (framework _legacyComponents/) is reclassified legacy, not version-unknown, since none of the deferred set carries a migrator to invent a version from | §2.8.4 | No _framework-props hand-authored; no full below 93; no version invented for a migrator-less widget | g-registry-completeness | n/a |
| D-115 | 2026-08-24 | decided | Scope change: WP-8 (the operating layer) splits into WP-8a (run-dir + 7 handoff schemas + sfs validate/run CLI + schemas.test + g-run-dir-location), WP-8b (6 runner/decide hooks + hooks.test + g-hook-contract/g-hook-liveness + push-admissible.test), WP-8c (3 agent files via git mv + .claude/settings.json + g-agent-contract/g-specwriter-purity/g-judge-isolation/g-plugin-enabled/g-markup-provenance), WP-8d (packages/mcp 7 tools/3 transports + .mcp.json + g-mcp-cli-parity + transports.test); session-scope-b.json swaps WP-8 for the four (§4.8 rows 1-8,12-13). §4.8 proves the hooks as programs so WP-8 is offline; the live restart pass is WP-11 (§4.9, out of session) | the operating layer is ~10 gates + 7 schemas + packages/mcp + 6 hooks + 3 agents, far past one context (CONTROL §6, mirrors D-103/D-106) | prove-b requires all four | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |
| D-114 | 2026-08-21 | decided | Scope change: WP-16b is removed from session-scope-b.json (18 -&gt; 17 ids). Its acceptance (g-brief-budget bundle enforced &lt;= 61440 B) is infeasible by the sanctioned BL-011 extraction: the docs/rebuild-brief/*.md bundle is ~613 KB and ~330 KB is design prose that cannot move to data/*.json, so 61440 B is reachable only by moving prose out of the glob (the S7 anti-pattern invariant 2 forbids) or a 6x rewrite dropping intent other WPs cite. BL-011 stays a deferred BACKLOG row, g-brief-budget stays bundle.enforced:false, and wp-table.json already carried WP-16b inScope:false | forcing the target would adapt the artifact to pass the check, the S7 failure this rebuild prevents | Scope B completes over the 17 achievable ids | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |
| D-111 | 2026-08-20 | decided | WP-6 raises the corpus round-trip 4-&gt;7 clean: the datatable-column decompile lifts an action column to a `do` (no empty bind/caption; a bind-less column's path is its index) and allows dotted refListRef names, and the action grammar gains openDialog argIgnore (version/customWidth/showCloseIcon) plus closeDialog/confirm/startEdit/cancelEdit intents and their `do`-enum entries — closing GAP-001; the four triaged forms escape only on the sectionSeparator/collapsiblePanel/tabs node-types (BL-024) | production dialog/edit/column action configs carried args and shapes the six-intent grammar could not express, so faithful forms escaped | g-escape-budget ratchets the escape rate 0.20-&gt;0.125 down-only | g-escape-budget | n/a |

## No theatre

```
T1  Shipping a script that cannot execute -> g-commands-executable
T2  A gate that warns where the docs say it fails -> g-gate-contract (mutations flip verdicts, not warning counts)
T3  A program that writes despite declaring it refuses to -> D-010 (structural:tokens.mjs)
T4  A pass with zero coverage -> structural:packages/registry/src/coverage.mjs (D-006)
T5  Claiming coverage of territory never visited -> g-coverage-single-impl + walk.mjs over items/columns/tabs
T6  Lazily-created report families that can vanish -> families() declared up front (D-005) + expectEmpty
T7  A gate wired into nothing -> the gate ratchet in run-gates.mjs (inputPaths must exist and be non-empty)
T8  A documented MUST-strength gate that ships no code -> g-commands-executable + D-002
T9  Resolving a contradiction by annotating it -> D-009 + g-prose-budget
T10 Changelog archaeology in an instruction file -> g-prose-budget archaeology patterns
T11 A prose rule with no enforcing program -> g-decisions (every row names a live or pending: enforcer)
T12 An unreproducible generated asset -> pending:WP-2 (pinned ref; no drive-letter path)
T13 Feeding the builder's self-report to the judge -> D-016, pending:BL-008
T14 Fuzzy matchers with escape hatches that downgrade failures -> D-011 + exact registry matching
T15 Presenting an unmeasurable quantity as a measurement -> D-014, D-050; unseeable is uninspectable
```
