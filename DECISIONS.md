## Decisions

| ID | Date | Status | Decision | Why | Consequence | Enforced by | Confirmation |
|---|---|---|---|---|---|---|---|
| D-011 | 2026-08-17 | decided | `validate-blocks.js` is deleted; its checks become T2 hard failures with exact registry matching | §1.4 | No fuzzy matcher with a downgrade | pending:WP-3a | n/a |
| D-017 | 2026-08-17 | decided | Fan out across screens only; never run parallel agents within one form | §4 L4 | Fan-out is a declared glob partition | pending:BL-008 | n/a |
| D-022 | 2026-08-17 | decided | Ambiguity in headless mode is resolved by an archetype default recorded in `plan.json` | §1.2#12 | Never ask, never silently pick | pending:BL-008 | n/a |
| D-030 | 2026-08-17 | pending-probe | `referenceListId` is always `{module, name}`; a module prefix inside `name` is illegal everywhere | §1.2#1 three-way split; observed `ConfigurationLoadingError` blocks the whole form | Flat and prefixed shapes hard-fail | pending:WP-3a | pending-probe:reflist-name-format |
| D-031 | 2026-08-17 | pending-probe | `refListStatus` carries `referenceListId: {module,name}` and nothing else; flat `module` / `referenceListName` keys hard-fail | §1.2#2 settled in one file while the routed-to file shipped the flat keys with a warning not to correct it | Two authors cannot ship two shapes | pending:WP-3a | pending-probe:reflist-status-shape |
| D-032 | 2026-08-17 | pending-probe | `editMode` is emitted only at form level, derived from `kind`: list/details -> `readOnly`, create/edit -> `editable`; no component carries it | §1.2#7 three answers to one question | SFS has no component-level field | pending:WP-3a | pending-probe:editmode-create-edit |
| D-033 | 2026-08-17 | pending-probe | The only legal `formArguments` hydration hook is `onAfterDataLoad`; `onDataLoaded` is illegal in compiler output | §1.2#8 three files, three hooks; the observed modal failure outranks prose | `forbidden` fails only when non-null | pending:WP-3a | pending-probe:form-arguments-hook |
| D-034 | 2026-08-17 | pending-probe | `actionOwner` is lowercase dotted or a compiler-resolved component id; `actionName` is the spaced form; SFS never names an owner | §1.2#9 the documented strings are the ones blamed for silent no-op buttons | The compiler derives it from the verb | pending:WP-3a | pending-probe:action-owner-casing |
| D-035 | 2026-08-17 | pending-probe | The `columns` component is banned unconditionally; multi-column layout is a flex `container` row sized by `desktop.dimensions.width` | §1.2#4 banned in five places, mandated in a sixth 52 lines earlier | No recipe, form or output contains one | pending:WP-3a | pending-probe:columns-alternative |
| D-036 | 2026-08-17 | pending-probe | Entity metadata is read from `Get` and unwrapped as `result.properties[]`; paths are PascalCase in metadata and camelCased when emitted | §1.2#10 wrong casing renders every cell blank | One unwrapper, one casing rule | check:t3-semantic:T3.02 | pending-probe:metadata-endpoint |
| D-049 | 2026-08-17 | superseded-by-D-109 | `verify-artifact.mjs` and `check-references.mjs` move unchanged, are referenced by nothing, and are registered in `quarantine.json` with `liftedBy: BL-003` | Phase 0 wants the bleeding stopped now, but the correct fixes need T2/T3 | A holed gate emits no green signal | g-check-references | n/a |
| D-055 | 2026-08-17 | decided | `forbidden` in `form-settings.json` means forbidden with a non-null value: fail iff the key is present and not `null`; fail on any key outside `allowed` union `forbidden` | §2.1.3 emits `onBeforeDataLoad: null` for every kind, which a literal reading would fail on the one artifact everything is measured against | A null forbidden key is legal | pending:WP-3a | n/a |
| D-089 | 2026-08-18 | superseded-by-D-095 | The `components-kb` and `capability-matrix.json` are NOT moved into `packages/registry/data`: WP-0's two disposition move rows are void per §2.8.1 ("the KB stays as the extractor's intermediate") and §2.8.5 (the matrix is a design-skill asset; only its `versions` map is deleted, D-087) | `gen-registry.mjs` reads the KB in place under `plugins/**`; relocating a 920 KB read-only input or a design-skill asset into L0 data would invert ownership | disposition.json carries no WP-2 move row; the KB and matrix keep their plugins paths | g-disposition | n/a |
| D-112 | 2026-08-21 | decided | Test change: the WP-9 precedent perf test's wall-clock budget is raised 50 -&gt; 120ms because `npm test` runs test files in parallel and this single reading absorbs co-runner scheduler/GC noise (measured 50-70ms under suite load, ~40ms in isolation); D-110's design intent and the algorithm's isolated cost are unchanged, and 120ms still fails hard on the O(n^2) regression the budget guards | §4.7.2 | No wall-clock assertion that flakes under the parallel runner | structural:packages/precedent/test/shape.test.mjs | n/a |
| D-113 | 2026-08-21 | decided | WP-2b lifts the registry from 12 to 93 `full` via the reproducible extractor parse-framework-props.mjs (source-parses each settings form: editor kind -&gt; valueType, omit-never-guess); `full &gt;= 93` and `deferredAuthorable &lt;= 7` are §2.8.4 demands; completenessOf counts framework-verified props as full-derived and a null overlay never clobbers a source-parsed prop; paragraph is reclassified legacy, not version-unknown | §2.8.4 | No _framework-props hand-authored; no full below 93; no invented version | g-registry-completeness | n/a |
| D-118 | 2026-08-24 | decided | Scope change: WP-8b (the 6 operating-layer hooks) splits into WP-8b.1/8b.2/8b.3 (per-sub-WP goals in wp-table.json); session-scope-b.json swaps WP-8b for the three | past one context (CONTROL §6, mirrors D-115) | prove-b requires all three | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |
| D-119 | 2026-08-24 | decided | WP-8b.1: Claude Code hooks return only exit 0 (decision on stdout) or 2 (stdout unwritable); exit(1) is banned (a non-2 non-zero exit is a silent non-block); decide(payload,ctx) is pure over an injected ctx so hooks.test runs it in-process (runner/decide split); block-form-writes is the fast first line of INV 1 (D-003), enforced of last resort by g-markup-provenance (WP-8c) | a hook that fails silently protects nothing (§4.3.1 rule 5) | g-hook-contract bans exit(1), checks every wired hook exists and every decide has a runner | g-hook-contract | n/a |
| D-120 | 2026-08-24 | decided | WP-8b.2: `sfs validate --schema N --file P [--json]` checks JSON against packages/sfs/schema/*.schema.json (exit 0/1/2, writes nothing); enforce-screen-lock (PreToolUse) is the fan-out mutex + contract precondition (one sfs-specwriter lock/screen, a signed 3+-predicate contract before code, a 3-round cap); validate-sfs-on-write (PostToolUse) spawns it, renames rejects to *.rejected, blocks HOOK-0002 if it cannot start | a second author per screen or unvalidated input revives the two-author/SFS-1002 defects | g-hook-contract + hooks.test prove both decides and the exit contract | g-hook-contract | n/a |
| D-121 | 2026-08-24 | decided | WP-8b.3: push admission is one program (packages/verify/src/bin/push-admissible.mjs, imports coverage.mjs) evaluating P0–P9; pushAdmissible(verdict,{allowPartial,present}) is the single T1–T3 rule (T1 pass; T2 pass or absent-component partial; T3 pass or --allow-partial backend/metadata partial). gate-push detects a push (incl. raw curl to UpdateMarkup) and maps its exit; gate-dispatch enforces judge isolation + paths-not-contents on Task; session-start prints six lines, never blocks; `sfs run lock/release/new` manages locks | admission rules in a hook are untestable and drift; a blocked SessionStart makes the repo unopenable | push-admissible.test proves P0–P9, g-hook-liveness proves hooks.jsonl covers events, hooks.test the three decides | g-hook-liveness | n/a |
| D-116 | 2026-08-24 | decided | WP-8a: the seven handoff schemas (plan/manifest/verdict/dispatch/sfs-meta/lock/blueprint in packages/sfs/schema/) are draft-2020-12, $id boxfusion.io/shesha/sfs/&lt;name&gt;.schema.json, additionalProperties:false at every object level, and ajv strict-compilable; the plan schema makes five defect classes structurally impossible: a screen with no contract, an all-visual contract, a prose assertion, a repair loop above 3 rounds, and two authors on one form | a prose/visual contract reintroduces the §1.2/§4L3 ambiguity and placement failures | schemas.test proves the seven compile under {strict:true} and the plan's five rejections | structural:packages/verify/test/schemas.test.mjs | n/a |
| D-117 | 2026-08-24 | decided | WP-8a: the run directory lives at runs/&lt;runId&gt;/, gitignored, never the pre-rebuild .claude/shesha/runs/ (§4.10 pt7) | a run written to the old path is invisible to the toolchain and to .gitignore, and an un-ignored runs/ commits ephemeral run output as if it were source | g-run-dir-location fails on old-path files or an unignored runs/ | g-run-dir-location | n/a |
| D-122 | 2026-08-24 | decided | Scope change: WP-8c (3 agents + 5 gates) splits into WP-8c.1 (6 agent files via git mv/rewrite + g-agent-contract/g-judge-isolation/g-plugin-enabled + enable plugin) and WP-8c.2 (g-markup-provenance closing D-003 + g-specwriter-purity + fixtures that recompile byte-identically + delete clean-form-config skill, moving its 8 assets/groups to packages/registry/data/prop-groups and repointing g-check-references); session-scope-b.json swaps WP-8c for the two | 6 agent files + 5 gates + generated compile fixtures past one context (CONTROL §6, mirrors D-115/D-118) | prove-b requires both | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |
| D-123 | 2026-08-24 | decided | WP-8c.1: the six subagents are files proved by g-agent-contract (name==basename, tools/disallowedTools present+disjoint, no MultiEdit, &lt;=4096 B, no "## Hard stops"); sfs-specwriter/sfs-evaluator are git mv of form-author/form-auditor, sfs-planner is new, the other three rewritten. g-judge-isolation keeps the self-report token &gt;400 chars from any evaluator/critic ref, pins dispatch.schema not.pattern, bars Bash from the evaluator; g-plugin-enabled requires enabledPlugins to enable shesha-developer | an agent rule addressed to a model's memory is not enforced | g-agent-contract + g-judge-isolation + g-plugin-enabled | g-agent-contract | n/a |
| D-124 | 2026-08-24 | decided | WP-8c.2: g-markup-provenance is the real INV-1 enforcer (closes D-003): it recompiles each compiled artifact's sibling SFS under its provenance meta (registrySha256/brand/compilerVersion) and hard-fails on any non-byte-identical result or missing sibling; fixtures are LF-pinned in .gitattributes. clean-form-config is deleted (shipped zero scripts, §1.7 T8); its 8 assets/groups move to packages/registry/data/prop-groups, read there by g-check-references | a command-line matcher cannot see a computed-path writer | g-markup-provenance recompiles every fixture; g-disposition records the move+deletes | g-markup-provenance | n/a |
| D-125 | 2026-08-24 | decided | WP-8c.2: g-specwriter-purity hard-fails any logs/specwriter-*.md carrying one of the six markup fingerprints (parentId, componentName, "desktop":, stylingBox, _type":"action-config, version-near-type); run-dirty is the negative control | quoting compiled markup means the author read the artifact, not the IR (§4.1.1) | g-specwriter-purity over the clean log + run-dirty | g-specwriter-purity | n/a |
| D-126 | 2026-08-24 | decided | WP-8d: the shesha-sfs MCP surface is seven tools (compile/decompile/verify/registry_lookup/metadata_entity/precedent_search/push) with ONE implementation in packages/mcp/src/tools/*.mjs behind 3 adapters — stdio JSON-RPC (@modelcontextprotocol/sdk 1.30.0, one external dep), loopback-only --http (any other --host exits 2), and a CLI subcommand per tool; /.mcp.json launches the stdio server; `sfs registry grammar` emits a GBNF whose header bars constraining a reasoning step | two surfaces with two implementations drift; a non-loopback bind exposes the tools | g-mcp-cli-parity locks the tool set == CLI subcommand set + schema-driven flags + the ESM export; transports.test proves the three transports | g-mcp-cli-parity | n/a |
| D-127 | 2026-08-28 | decided | WP-3c: T4/T4b live at packages/verify/src/tiers/ and T4b reports inside tiers.T4 (the envelope has no T4b key); both dispose notRun with a BLOCKED row, never uninspectable; T4b.02 needs BOTH overlap extents > 1px; the tier result enum gains notRun; quarantine/ is emptied | rounded rects make two abutting boxes report a full-width 1px sliver (measured on the recorded capture) | A tier that could not look says so and is never a pass | g-blocked-honesty | n/a |
| D-128 | 2026-08-28 | decided | WP-3d: T5 is a ranking signal — 5 orthogonal rubric axes as a vector with no total computed anywhere, a judge disqualified below 9-of-10 anchor-first, a 3-round cap, an injected judge so every path runs with no model; the judge-truth ledger must STATE the number, and a zero-screen entry must name its blocker | a gap you cannot state is a gate you cannot trust | T5 never changes result or the exit code | g-t5-advisory | n/a |
| D-115 | 2026-08-24 | decided | Scope change: WP-8 (the operating layer) splits into WP-8a (run-dir + 7 handoff schemas + sfs validate/run CLI + schemas.test + g-run-dir-location), WP-8b (6 runner/decide hooks + hooks.test + g-hook-contract/g-hook-liveness + push-admissible.test), WP-8c (3 agent files via git mv + .claude/settings.json + g-agent-contract/g-specwriter-purity/g-judge-isolation/g-plugin-enabled/g-markup-provenance), WP-8d (packages/mcp 7 tools/3 transports + .mcp.json + g-mcp-cli-parity + transports.test); session-scope-b.json swaps WP-8 for the four (§4.8 rows 1-8,12-13). §4.8 proves the hooks as programs so WP-8 is offline; the live restart pass is WP-11 (§4.9, out of session) | far past one context (CONTROL §6, mirrors D-103/D-106) | prove-b requires all four | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |
| D-114 | 2026-08-21 | decided | Scope change: WP-16b is removed from session-scope-b.json (18 -&gt; 17 ids). Its acceptance (g-brief-budget bundle enforced &lt;= 61440 B) is infeasible by the sanctioned BL-011 extraction: the docs/rebuild-brief/*.md bundle is ~613 KB and ~330 KB is design prose that cannot move to data/*.json, so 61440 B is reachable only by moving prose out of the glob (the S7 anti-pattern invariant 2 forbids) or a 6x rewrite dropping intent other WPs cite. BL-011 stays a deferred BACKLOG row, g-brief-budget stays bundle.enforced:false, and wp-table.json already carried WP-16b inScope:false | forcing the target would adapt the artifact to pass the check, the S7 failure this rebuild prevents | Scope B completes over the 17 achievable ids | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |

## No theatre

```
T1  Shipping a script that cannot execute -> g-commands-executable
T2  A gate that warns where the docs say it fails -> g-gate-contract (mutations flip verdicts, not warning counts)
T3  A program that writes despite declaring it refuses to -> D-010 (structural:tokens.mjs)
T4  A pass with zero coverage -> structural:packages/registry/src/coverage.mjs (D-006)
T5  Claiming coverage of territory never visited -> g-coverage-single-impl + walk.mjs over items/columns/tabs
T6  Lazily-created report families that can vanish -> families() declared up front (D-005) + expectEmpty
T7  A gate wired into nothing -> the gate ratchet in run-gates.mjs (inputPaths must exist and be non-empty)
T8  A documented MUST-strength gate that ships no code -> g-commands-executable + D-002
T9  Resolving a contradiction by annotating it -> D-009 + g-prose-budget
T10 Changelog archaeology in an instruction file -> g-prose-budget archaeology patterns
T11 A prose rule with no enforcing program -> g-decisions (every row names a live or pending: enforcer)
T12 An unreproducible generated asset -> pending:WP-2 (pinned ref; no drive-letter path)
T13 Feeding the builder's self-report to the judge -> D-016, pending:BL-008
T14 Fuzzy matchers with escape hatches that downgrade failures -> D-011 + exact registry matching
T15 Presenting an unmeasurable quantity as a measurement -> D-014, D-050; unseeable is uninspectable
```
