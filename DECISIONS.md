## Decisions

| ID | Date | Status | Decision | Why | Consequence | Enforced by | Confirmation |
|---|---|---|---|---|---|---|---|
| D-003 | 2026-08-17 | decided | The compiler is the only writer of form markup; direct writes are blocked, not discouraged | §4 L4 | No hand-edited form JSON | pending:BL-008 | n/a |
| D-011 | 2026-08-17 | decided | `validate-blocks.js` is deleted; its checks become T2 hard failures with exact registry matching | §1.4 | No fuzzy matcher with a downgrade | pending:WP-3a | n/a |
| D-014 | 2026-08-17 | decided | Placement assertions are executable predicates over the compiled tree, not English sentences judged by a model | §4 L3 | The DOM probe covers residue only | pending:BL-003 | n/a |
| D-015 | 2026-08-17 | decided | T5 visual is advisory: `verdict.result` is computed from T1-T4 only and is byte-identical whichever score T5 reports | §4 L3 | No T5 finding downgrades a result | pending:BL-005 | n/a |
| D-016 | 2026-08-17 | decided | The judge never sees the builder's self-report or reasoning | §4 L3 | The Evaluator cannot read run logs | pending:BL-008 | n/a |
| D-017 | 2026-08-17 | decided | Fan out across screens only; never run parallel agents within one form | §4 L4 | Fan-out is a declared glob partition | pending:BL-008 | n/a |
| D-022 | 2026-08-17 | decided | Ambiguity in headless mode is resolved by an archetype default recorded in `plan.json` | §1.2#12 | Never ask, never silently pick | pending:BL-008 | n/a |
| D-030 | 2026-08-17 | pending-probe | `referenceListId` is always `{module, name}`; a module prefix inside `name` is illegal everywhere | §1.2#1 three-way split; observed `ConfigurationLoadingError` blocks the whole form | Flat and prefixed shapes hard-fail | pending:WP-3a | pending-probe:reflist-name-format |
| D-031 | 2026-08-17 | pending-probe | `refListStatus` carries `referenceListId: {module,name}` and nothing else; flat `module` / `referenceListName` keys hard-fail | §1.2#2 settled in one file while the routed-to file shipped the flat keys with a warning not to correct it | Two authors cannot ship two shapes | pending:WP-3a | pending-probe:reflist-status-shape |
| D-032 | 2026-08-17 | pending-probe | `editMode` is emitted only at form level, derived from `kind`: list/details -> `readOnly`, create/edit -> `editable`; no component carries it | §1.2#7 three answers to one question | SFS has no component-level field | pending:WP-3a | pending-probe:editmode-create-edit |
| D-033 | 2026-08-17 | pending-probe | The only legal `formArguments` hydration hook is `onAfterDataLoad`; `onDataLoaded` is illegal in compiler output | §1.2#8 three files, three hooks; the observed modal failure outranks prose | `forbidden` fails only when non-null | pending:WP-3a | pending-probe:form-arguments-hook |
| D-034 | 2026-08-17 | pending-probe | `actionOwner` is lowercase dotted or a compiler-resolved component id; `actionName` is the spaced form; SFS never names an owner | §1.2#9 the documented strings are the ones blamed for silent no-op buttons | The compiler derives it from the verb | pending:WP-3a | pending-probe:action-owner-casing |
| D-035 | 2026-08-17 | pending-probe | The `columns` component is banned unconditionally; multi-column layout is a flex `container` row sized by `desktop.dimensions.width` | §1.2#4 banned in five places, mandated in a sixth 52 lines earlier | No recipe, form or output contains one | pending:WP-3a | pending-probe:columns-alternative |
| D-036 | 2026-08-17 | pending-probe | Entity metadata is read from `Get` and unwrapped as `result.properties[]`; paths are PascalCase in metadata and camelCased when emitted | §1.2#10 wrong casing renders every cell blank | One unwrapper, one casing rule | pending:BL-003 | pending-probe:metadata-endpoint |
| D-049 | 2026-08-17 | decided | `verify-artifact.mjs` and `check-references.mjs` move unchanged, are referenced by nothing, and are registered in `quarantine.json` with `liftedBy: BL-003` | Phase 0 wants the bleeding stopped now, but the correct fixes need T2/T3 | A holed gate emits no green signal | pending:BL-003 | n/a |
| D-055 | 2026-08-17 | decided | `forbidden` in `form-settings.json` means forbidden with a non-null value: fail iff the key is present and not `null`; fail on any key outside `allowed` union `forbidden` | §2.1.3 emits `onBeforeDataLoad: null` for every kind, which a literal reading would fail on the one artifact everything is measured against | A null forbidden key is legal | pending:WP-3a | n/a |
| D-058 | 2026-08-17 | decided | Fan-out only for disjoint-write, no-shared-invariant, program-verifiable, enumerable slices, max 4 agents; subagents never commit and never mutate `node_modules` or the index | §4/L4 excludes shared-context coding from multi-agent gains; two parallel authors already shipped two incompatible `refListStatus` shapes | A self-report is not evidence | g-fanout-partition | n/a |
| D-059 | 2026-08-17 | decided | Gate removal: `g-fanout-discipline`'s two-directory rule | it failed every commit shipping an implementation together with the gate policing it, i.e. WP-1, WP-5, WP-7a and WP-3a | The declared-glob partition proof already covers the anti-pattern | g-fanout-partition | n/a |
| D-072 | 2026-08-17 | decided | The plugin version is valid semver including prerelease identifiers, so `1.8.5-alpha.1` and never `1.8.5alpha1` | `g-plugin-version` must prove the version strictly increases and a semver parser rejects an unseparated prerelease suffix | Marketplace and gate order it the same way | g-plugin-version | n/a |
| D-089 | 2026-08-18 | superseded-by-D-095 | The `components-kb` and `capability-matrix.json` are NOT moved into `packages/registry/data`: WP-0's two disposition move rows are void per §2.8.1 ("the KB stays as the extractor's intermediate") and §2.8.5 (the matrix is a design-skill asset; only its `versions` map is deleted, D-087) | `gen-registry.mjs` reads the KB in place under `plugins/**`; relocating a 920 KB read-only input or a design-skill asset into L0 data would invert ownership | disposition.json carries no WP-2 move row; the KB and matrix keep their plugins paths | g-disposition | n/a |
| D-090 | 2026-08-18 | decided | Scope change: WP-5 splits into WP-5.a (compiler v1 — six stages, all node kinds, seven recipes, error catalogue, decompiler lifts) and WP-5.b (decompiler over the six declared corpus forms, round-trip >= 0.90, the five WP-5 gates); both stay in scope | WP-5's compiler-all-kinds surface plus the corpus round-trip exceeds one context window and CONTROL section 6 forbids half-committing, so the split is data declared before the work, mirroring D-070 | ten ids in scope; no work leaves the session | structural:packages/verify/config/session-scope.json | n/a |
| D-091 | 2026-08-18 | decided | Test change: the golden-defect predicates are kind-aware. N1 exempts labelled INPUT types (registry `editModeChannel:"input"`) not just SFS node `field`; N8 infers the kind from the emitted loader/submitter signature and checks the matching profile rather than hardcoding the list's `dataSubmitterType:"none"`; A1's `items` non-vacuity moves from per-fixture to SET-level (like `slots`, D-080) | the predicates were written when only the list golden existed and baked in list expectations; create/detail/datalist forms satisfy the SAME rules with different profile values, and an input legitimately shows its label | the N1..N11 rules and A1..A5 identities hold for every kind; non-vacuity of slots AND items is asserted across the fixture set | structural:packages/sfs/test/predicates.mjs | n/a |
| D-092 | 2026-08-18 | decided | The error catalogue (§2.7) is GENERATED from `packages/sfs/src` by `tools/gen-catalogue.mjs` — one row per `new <X>Error(code, message)` — and a test asserts byte-identity to a fresh generation plus coverage of every raised code | a prose list of error codes rots the first time a code is added; deriving it from the raise sites makes drift a failing test (INV-2) | 34 codes catalogued; a new code with no row fails `catalogue.test.mjs` | structural:packages/sfs/test/catalogue.test.mjs | n/a |
| D-093 | 2026-08-18 | decided | Node-kind resolution for the input-ish leaves: `field` resolves its type from `component` (offline; `_datatypeMap` needs a backend, MET-2203); `select` resolves to `dropdown` (referenceList) or `autocomplete` (entity) by `source`; the decompiler lifts `dropdown`/`autocomplete` back to `select` so the emit path — and thus the type-specific key order — is identical on round-trip | the SFS node enum stays fixed while any registry input type is reachable via `field`; lifting dropdown to field+raw would reorder type-specific keys and break Q1 byte-equality | all six new node kinds round-trip byte-equal (selfconsistency over the clean set) | structural:packages/sfs/src/compile/s2-resolve.mjs | n/a |
| D-094 | 2026-08-19 | decided | Gate removal: WP-5's five planned gates resolve as — g-corpus-immutable and g-escape-budget shipped; g-determinism's enforcement is `determinism.test.mjs` (D-021) and g-no-literal-hex's is `tokens.mjs` TOK-2010 (D-010/D-019), both already-live programs not duplicated as gates; g-markup-provenance is deferred to BL-008 (no committed `*.form.json` artifacts exist in Scope A to recompile) | INV-2 is satisfied when a rule has A program enforcing it, not necessarily a NEW gate; duplicating live enforcement as gates adds theatre, and a provenance gate with nothing to walk is zero-coverage | every pending:WP-5 decision now names a live enforcer; two net-new gates ship | structural:packages/verify/config/pending-budget.json | n/a |
| D-095 | 2026-08-19 | decided | The `components-kb` relocates to `packages/sfs/kb` and the examples to `packages/sfs/corpus` (both L1) as WP-7a deletes `shesha-form-edit`; both stay read-only extractor/round-trip inputs, now self-contained under `packages/` with no toolchain code reaching into a plugin skill. `capability-matrix.json` is unaffected (D-087). Supersedes D-089 | D-089 kept the KB under plugins/** to avoid inverting ownership into L0 registry/data, but deleting the skill that hosted it forces a home, and packages/sfs (the compiler+registry toolchain) is its true owner — L1 does not invert ownership | gen-registry.mjs reads the KB at its new path; the registry regenerates byte-identical; disposition carries WP-7a move rows for both | g-disposition | n/a |
| D-096 | 2026-08-19 | decided | Scope change: WP-3a splits into WP-3a.1 (the verifier registry read-API `load()` + `slots.json`/`required-props.json`/`deny.json` and the single tree walker `walk.mjs`) and WP-3a.2 (T1 schema tier, T2 registry 22 checks, `verify.mjs`, the coverage gates); both stay in scope. `WP-3a` is kept in wp-table as an inScope:false umbrella so the eight pending:WP-3a T2/verify decisions still resolve until WP-3a.2 ships their enforcers | WP-3a.1 read-layer+walker is green and committable now, but T2 22 checks + verify + gates + 42 fixtures exceed one context; CONTROL §6 forbids half-committing and blesses the WP-N.a/.b split with a Scope change row (mirrors D-070, D-090) | eleven ids in session-scope; no work leaves the session; prove.mjs requires WP-3a.1 and WP-3a.2 | structural:packages/verify/config/session-scope.json | n/a |
| D-097 | 2026-08-19 | decided | Scope change: WP-3a.2 completes the registry for the non-priority types the clean/corpus forms use (container, dataContext, card, text, ...), pulling that slice of BL-004/BL-020 into scope — gen-registry reads `kb.settingsFields[].path` names and MERGES authored props; container gains alignItems/flexWrap/direction and dataContext uniqueStateId, all verified present in the framework clone at the pinned commit. T2 dispositions: T2.17 na (subsumed by TOK-2010, output provenance is BL-023), T2.15 flags only non-empty-base stylingBox dup, T2.20 reads the recorded meta.kind and treats none/null/empty as inactive | T2 cannot conform against clean compiler output while the registry lacks props the compiler legitimately emits; the brief schedules WP-3a last precisely because T2 needs the completed registry, and degrading non-priority T2.04 to uninspectable would make T2 partial and fail its acceptance | verify --tiers t1,t2 on the clean form exits 0; validate 121/121, priority full 13/13 | structural:packages/verify/test/t2-registry.test.mjs | n/a |
| D-098 | 2026-08-19 | decided | Scope change: WP-3a splits a second time (after D-096) — WP-3a.2 is the tier ladder (T1 + T2 + verify.mjs), WP-3a.3 is the coverage-proof layer (g-exit-codes, g-mutation-coverage, g-defect-class-coverage, defect-classes.json, the mutation-harness kind:fixture extension, and the tier fixtures); both stay in scope | the tier ladder is green, tested, and passes verify --tiers t1,t2 -> exit 0 now, while the coverage gates + fixtures + harness extension are a further context; CONTROL §6 blesses the WP-N.a/.b split for a WP exceeding one context and WP-3a is the acknowledged-largest (mirrors D-070, D-090, D-096) | twelve ids in session-scope; no work leaves the session; prove.mjs requires WP-3a.2 and WP-3a.3 | structural:packages/verify/config/session-scope.json | n/a |
| D-099 | 2026-08-19 | decided | The verifier tiers ship a coverage-proof layer: every tier `checks[]` id is covered by a verdict-flipping `mutations[].covers` entry or is explicitly `subsumed` (g-mutation-coverage, proven flipping by tier-mutations.test); the >=90% defect-class claim is falsifiable via `defect-classes.json`, each class named to the check that catches it (g-defect-class-coverage); exit codes and the verdict union {pass,fail,partial,notRun} are single-sourced, no `warn` (g-exit-codes) | §3.5.1 a check class with no mutation does not exist; a defect-class claim with no covering check is unfalsifiable; a fifth verdict is a fail that stays green | 31 tier mutations all flipping; three coverage gates live; roster 20 | g-mutation-coverage | n/a |
| D-100 | 2026-08-19 | decided | Scope change: open Scope B. Scope-B WP ids live in a new packages/verify/config/session-scope-b.json read only by a new packages/verify/src/prove-b.mjs; session-scope.json stays frozen at the 12 Scope-A ids so the Scope-A proof and its frozen expected file never drift. Adds WP-0b/5c/5d/5e/2b/1c/16b to wp-table.json and a Scope-B block to CONTROL §3. Scope B is done when prove-b prints SESSION COMPLETE — SCOPE B, granted exactly one --bless of prove-b.expected.txt, additive to CONTROL §5's two spent Scope-A blesses | the mining run measured 7.0% round-trip over 2,071 real forms, so the backlog is real work the rebuild owes; leaving it prose is the failure invariant 2 forbids | Scope A's frozen proof and bless budget are untouched; Scope B carries its own | structural:packages/verify/test/prove-b.contract.test.mjs | n/a |

## No theatre

```
T1  Shipping a script that cannot execute -> g-commands-executable
T2  A gate that warns where the docs say it fails -> g-gate-contract (mutations flip verdicts, not warning counts)
T3  A program that writes despite declaring it refuses to -> D-010 (structural:tokens.mjs)
T4  A pass with zero coverage -> structural:packages/registry/src/coverage.mjs (D-006)
T5  Claiming coverage of territory never visited -> g-coverage-single-impl + walk.mjs over items/columns/tabs
T6  Lazily-created report families that can vanish -> families() declared up front (D-005) + expectEmpty
T7  A gate wired into nothing -> the gate ratchet in run-gates.mjs (inputPaths must exist and be non-empty)
T8  A documented MUST-strength gate that ships no code -> g-commands-executable + D-002
T9  Resolving a contradiction by annotating it -> D-009 + g-prose-budget
T10 Changelog archaeology in an instruction file -> g-prose-budget archaeology patterns
T11 A prose rule with no enforcing program -> g-decisions (every row names a live or pending: enforcer)
T12 An unreproducible generated asset -> pending:WP-2 (pinned ref; no drive-letter path)
T13 Feeding the builder's self-report to the judge -> D-016, pending:BL-008
T14 Fuzzy matchers with escape hatches that downgrade failures -> D-011 + exact registry matching
T15 Presenting an unmeasurable quantity as a measurement -> D-014, D-050; unseeable is uninspectable
```
